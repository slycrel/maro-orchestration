# Bake-off sandbox package — candidate MemoryStore adapters live here.
# Nothing in src/ imports this; the contract suite loads adapters via the
# MEMORY_BAKEOFF_ADAPTER env hook (see tests/test_memory_port.py).
