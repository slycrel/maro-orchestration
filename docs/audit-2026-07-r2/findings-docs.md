# Purgatorio r2 Eye 4 — Docs coherence, post-fix-wave

Probed 2026-07-10, read-only (this findings file is the only write). Frame
unchanged from r1: **a stranger clones Maro at 1.0 and reads the docs — do
they end up with a true picture?** Every status below was re-probed against
the tree this session (file:line / command cited); no commit message or doc
claim was accepted as resolution evidence. Fix-wave under test:
`4e6dc1b..HEAD` (21 commits), especially 1d3b77e (SECURITY_MODEL rewrite),
83ede86 (README reposition), bf144fc + 7c1086c (user/ overlay), d6c143b
(supervision shim), 34241d9 (PUBLISH_CHECKLIST as living gate).

## What matters most now

1. **The supervision story is still broken — the fix moved the hole**
   (docs-r2-01): d6c143b deleted bootstrap's service-file generator (good:
   the crash-looping `sheriff.py --heartbeat` unit is gone, docs-03's core),
   but README's "Optional Services" still tells a stranger to
   `sudo cp ~/.maro/workspace/deploy/systemd/maro-{heartbeat,telegram,inspector}.service`
   — a directory **nothing creates anymore** (`config.deploy_dir()` now has
   zero callers; bootstrap.py contains zero references to "deploy"), and two
   of the three unit files exist **nowhere in the repo at all**. README:237
   even says the unit is "generated by `maro-bootstrap services`" while
   bootstrap.py:267's own help text says "Maro installs no unit files".
2. **The newly-documented config lane over-claims: 4 of 10 documented keys
   are dead** (docs-r2-02): `research_step_model`, `max_steps`,
   `always_skeptic`, `notify_on_complete` are documented with concrete
   effects in user/CONFIG.md, user/README.md, and DEFAULTS.md — and no code
   anywhere reads them (repo-wide grep = 0; `git log -S` shows they never
   existed in src/). Silently-ignored settings are the exact failure class
   docs-08 was about.
3. **SF-5 privacy, honestly stated:** the tip is genuinely clean (neutral
   templates, overlay-first resolution verified in all five readers), but
   Jeremy's medical data is still in the git history of a repo that is
   **PUBLIC right now** (`gh repo view` → PUBLIC; `git show
   61ef0a3:user/GOALS.md` still yields it). This is a Jeremy-decreed
   deferral (decisions doc 2026-07-09) and is gated in PUBLISH_CHECKLIST:18,
   and user/README's privacy note discloses it — recorded here as
   partially-resolved, not re-raised as a new blocker.

## Part A — prior findings re-verified

| id | r1 severity | current status | probe evidence | note |
|---|---|---|---|---|
| docs-01 | blocker | **resolved** | docs/SECURITY_MODEL.md full read (rewritten 1d3b77e): "There is no sandbox on the live executor path" (:16), sandbox.py called an "unwired prototype" (:31-35). Spot-verified its positive claims against code: `--dangerously-skip-permissions` + `--tools ""` / `--disallowedTools` at src/llm.py:1007-1018; MARO_WORKER_RUN at src/llm.py:649; budget caps at src/loop_init.py:30-31; `maro sandbox` subcommand exists (src/cli.py:1846, dispatch :2123); run_skill_sandboxed still has zero production callers (grep). Corpus sweep for residual "runs sandboxed"-type claims: only the self-referential correction note (SECURITY_MODEL.md:143). GOAL_BRAIN.md:1435-1436 contains the dockerize quote the doc cites. | Minor line-number drift only (doc says cli.py:1764 → now 1846; metrics.py:193 → now 200); substantively accurate |
| docs-02 | blocker | **partially-resolved** | Tip: `git ls-files user/` = 6 files, all neutral — grep for retatrutide/tirzepatide/nootropic/BPC-157/epitalon/edgar_allen/jeremy/slycrel over user/ = CLEAN. Overlay-first everywhere via config.py:97 `user_file()`: planner.py:361-371 (GOALS/CONTEXT/SIGNALS), evolver_scans.py:106-107, handle.py:471-472 (CONFIG.md), heartbeat.py:972-973; real files confirmed at ~/.maro/workspace/user/. README:356 + user/README.md document the lane and the "sent to your model provider" warning. History: `git show 61ef0a3:user/GOALS.md` still contains the medical terms and `gh repo view slycrel/maro-orchestration` → **PUBLIC** | History exposure remains by informed decree (docs/history/2026-07-09-decisions-for-jeremy.md:113-122: Jeremy will personally review; "history stays as-is until that review") and is a PUBLISH_CHECKLIST:18 gate; user/README privacy note honestly discloses it |
| docs-03 | blocker | **partially-resolved** | The broken generated unit is gone: bootstrap.py:126-162 now only *prints* host-scheduler hook instructions ("Maro never installs its own daemon or timer"; the old `sheriff.py --heartbeat` exec is memorialized in a comment at :126-131); `maro-bootstrap services` help = "Print host-scheduler hook instructions (Maro installs no unit files)" (:267). BUT README's Optional Services was not updated to match (→ docs-r2-01) and both deploy/systemd/ units survive (heartbeat unit hardcodes /home/clawd paths; observe unit execs an archived stub → docs-r2-04) | "Pick ONE supervision story" still unmet: bootstrap says instructions-only, README says cp-generated-units, deploy/ ships a third story |
| docs-04 | real-but-deferrable | **resolved** | DEFAULTS.md:17-26 now documents the `user/CONFIG.md` lane ("A second, separate lane (SF-5/docs-04)"), names its keys, states it resolves workspace-overlay-first since 2026-07-10, and explicitly acknowledges the census blind spot ("The census below structurally cannot see this lane"); full key table in user/README.md; README:356 links it. `yolo` discoverable in docs now | The lane is documented — but the documentation over-claims dead keys (new finding docs-r2-02) |
| docs-05 | real-but-deferrable | **resolved** | CLAUDE.md:163: "`~/.maro/config.yml` \| User-level \| Model prefs, notifications (API keys stay in env or `secrets/.env`; `yolo` lives in `user/CONFIG.md` / `MARO_YOLO`)" — matches config.py credential discovery and handle.py:1011 yolo read | |
| docs-06 | real-but-deferrable | **resolved** | README rewritten (83ede86): line 30 routes to "exactly what fires today vs what's still experimental"; lines 447-451: "**Designed, not production-proven**... gated behind heartbeat autonomy and **has never fired in production**". The old "every 10 minutes" ambient-default claims are gone | |
| docs-07 | real-but-deferrable | **resolved** | skills/arch-platform.md:17-20 now reads "Try in order: AnthropicSDK → ClaudeSubprocess → OpenRouter → OpenAI (DEFAULT_BACKEND_ORDER in llm.py — subprocess is SECOND, not last)" — matches src/llm.py:1667 | |
| docs-08 | real-but-deferrable | **resolved** | `grep -rn POE_ENV_FILE src/ docs/ README.md` → hits only in docs/history/ (point-in-time records, correct for their date) and the r1 audit records themselves; SECURITY_MODEL's rewritten Secrets section names `config.load_credentials_env()` and `<workspace>/secrets/.env` only | |
| docs-09 | cosmetic | **partially-resolved** | SECURITY_MODEL.md: poe-orchestration ghost + "poe-security audit" gone (full read). CONTRIBUTING.md:3 STILL reads: Thanks for helping improve `openclaw-orchestration` | One line left |
| docs-10 | cosmetic | **resolved** | `grep -n maro-observe CLAUDE.md` = 0 hits; Running-things block no longer advertises the archived dashboard | |
| docs-11 | cosmetic | **obsolete** | Premise fails on probe: `git ls-files memory/` = **0 files**, `git log --oneline --full-history -- memory/` = **empty** (memory/ is gitignored, .gitignore:17, and was never tracked on this branch's history — no untracking commit exists in the fix window either). The r1 evidence claim "`git ls-files memory/` includes memory/step-costs.jsonl" does not reproduce and appears to have been wrong; a fresh clone gets no stale telemetry | Residual cosmetic (recorded here, not filed): README:34,193 examples pass `memory/step-costs.jsonl`, which won't exist on a fresh clone (loud missing-file error, not silent stale data) |

## Part B — new findings (r2 sweep)

| id | claim | evidence | subsystem | severity |
|---|---|---|---|---|
| docs-r2-01 | README "Optional Services" is broken end-to-end after the supervision-shim rewrite: it instructs `sudo cp ~/.maro/workspace/deploy/systemd/maro-{heartbeat,telegram,inspector}.service` but (a) nothing creates that directory — bootstrap.py has zero "deploy" references, `config.deploy_dir()` (config.py:303) has **zero callers**, and the dir doesn't exist even on this long-running box; (b) `maro-telegram.service` and `maro-inspector.service` exist **nowhere** (repo deploy/systemd/ holds only maro-heartbeat + maro-observe; grep over src/ = 0); (c) README:237 says the unit is "generated by `maro-bootstrap services`" while bootstrap.py:267 says "Print host-scheduler hook instructions (Maro installs no unit files)". A stranger following the README hits three cp failures; the doc directly contradicts the code shipped the same day. d6c143b deleted the generator without updating README (the cp lines predate the wave per `git blame -L 283,300`) | README.md:237, 290-298; src/bootstrap.py:126-162, 265-269; src/config.py:303-312; `grep -rn deploy_dir src/` (only definition); `ls ~/.maro/workspace/deploy/systemd/` → missing | docs / platform | blocker-for-1.0 |
| docs-r2-02 | The newly-documented user/CONFIG.md lane over-claims four dead keys: `research_step_model`, `max_steps`, `always_skeptic`, `notify_on_complete` are documented with concrete effects in user/CONFIG.md (template body), user/README.md (key table with defaults), and docs/DEFAULTS.md:21-24 — but **no code reads any of them**: repo-wide `grep -rn` over src/ = 0 hits; `git log -S` per key over src/ = empty (never existed); handle.py's actual `_cfg.get` consumers are only default_model_tier/yolo/ralph_verify/quality_gate/quality_gate_action (handle.py:740,1011,1142,1842,1863) + mcp_servers (heartbeat.py:972); `classify_step_model` (conductor.py:159) is a pure keyword heuristic that never consults config. An operator setting `always_skeptic: true` or `notify_on_complete: false` gets a silently ignored setting — the docs-08 failure class, reintroduced by the lane's own documentation | user/README.md key table; user/CONFIG.md; docs/DEFAULTS.md:21-24; greps + `git log -S` above | docs / interface | real-but-deferrable |
| docs-r2-03 | The shipped user/CONFIG.md template contradicts its own fix wave: its header NOTE says "unlike GOALS/CONTEXT/SIGNALS... CONFIG.md is currently read from the repo/install copy only. Overlay migration for this file is queued" — but 7c1086c (same wave, next day) made CONFIG.md overlay-first, and user/README.md says the caveat "was closed 2026-07-10"; the template also lists `research_step_model` among "Readers today" (false, see docs-r2-02). bf144fc wrote the note; 7c1086c fixed the code without updating it | user/CONFIG.md header NOTE block + "Readers today" list; user/README.md "Resolution order" section; src/handle.py:471-472 | docs | cosmetic |
| docs-r2-04 | deploy/systemd/ ships two stale units contradicting the new "Maro installs no unit files" posture: maro-observe.service execs `python3 -m observe serve --port 80` — an archived stub (observe.py:635 "[ARCHIVED] see archive/observe_dashboard.py"); maro-heartbeat.service hardcodes `WorkingDirectory=/home/clawd/claude/maro-orchestration` — broken on any machine but this one | deploy/systemd/maro-observe.service:9-10; deploy/systemd/maro-heartbeat.service:15-16; src/observe.py:635; src/bootstrap.py:267 | docs / platform | cosmetic |
| docs-r2-05 | Stranger-facing README still contains the owner's name as operational fact: "Telegram escalation: critical health → alert Jeremy" — PUBLISH_CHECKLIST's own (unchecked) gate says "Examples use generic placeholders (no personal account data)" | README.md:305; docs/PUBLISH_CHECKLIST.md "Security and privacy review" | docs | cosmetic |
| docs-r2-06 | PUBLISH_CHECKLIST.md was adopted as *the* living 1.0 gate (34241d9) but is absent from docs/INDEX.md's question→doc map — a stranger (or next session) asking "what gates a 1.0 publish?" finds no row; INDEX is the doc that carries the status legend and routes every other living doc | docs/INDEX.md (grep CHECKLIST = 0 hits; 31 table rows); docs/PUBLISH_CHECKLIST.md:1-9 (status: living, "the 1.0 gate scaffold") | docs | cosmetic |

## Clean checks (probed, no finding)

1. SECURITY_MODEL rewrite spot-verified against code — executor flags
   (llm.py:1007-1018), worker push guard env (llm.py:649), budget defaults
   (loop_init.py:30-31), sandbox inventory (cli.py:1846 manual subcommand,
   skills.py:1728 off-by-default flag, zero production callers) — all hold;
   only cosmetic line-number drift.
2. GOAL_BRAIN.md:1435-1436 contains the dockerize-executor decision
   SECURITY_MODEL Part 2 quotes; the two docs agree.
3. user/ shipped templates grep-clean of personal/medical identifiers
   (retatrutide, tirzepatide, nootropic, BPC-157, epitalon, edgar_allen,
   jeremy, slycrel → 0 hits).
4. All five user/ readers resolve through config.user_file() overlay-first
   (planner.py:361-371, evolver_scans.py:106, handle.py:471 + 1243,
   heartbeat.py:972); ~/.maro/workspace/user/ holds Jeremy's real files, so
   this box's behavior is preserved post-move.
5. user/README.md privacy note honestly discloses the git-history exposure
   ("Fixing the tip does not scrub history") — coherent with
   PUBLISH_CHECKLIST:18 and the 2026-07-09 decisions record.
6. README now carries an explicit prototype-status disclaimer ("personal
   infrastructure / active development... go in eyes open") — the checklist's
   scope gate is satisfiable as written.
7. README self-improvement claims match implementation posture: evolver
   meta-cycle labeled "Designed, not production-proven... has never fired in
   production" (README:447-451); heartbeat/evolver repo-layout rows say
   opt-in/experimental (:392-393).
8. skills/arch-platform.md backend order matches llm.py:1667 (subprocess
   second), including an explicit "SECOND, not last" callout.
9. DEFAULTS.md documents the CONFIG.md lane and admits the census can't see
   it; tests/test_docs_frontmatter.py passes post-wave (5 passed, single-file
   run).
10. README benchmarking commands reference real surfaces: maro-tool-costs =
    tool_cost_report:main (pyproject:60, src/tool_cost_report.py exists);
    benchmarks/fixture-workloads.json is git-tracked.
11. PUBLISH_CHECKLIST internally coherent: PyPI name claim matches
    pyproject.toml's `maro-orchestration`; history-review deferral wording
    matches the decisions record.
12. docs/INDEX.md still routes "Security / sandbox posture?" →
    SECURITY_MODEL.md (the rewritten doc is reachable from the map).

## Notes for reconciliation

- docs-r2-01 + docs-r2-04 + the docs-03 residue are one item: the
  supervision story now has three mutually contradictory tellings
  (bootstrap prints hook instructions; README cps generated units that are
  never generated; deploy/ ships one hardcoded unit and one archived-stub
  unit). Finishing d6c143b's story — rewrite README "Optional Services" to
  the host-scheduler-hook model and delete/genericize deploy/systemd/ —
  closes all three at once.
- docs-r2-02 + docs-r2-03 are the user/-lane documentation debugged against
  itself: either wire the four dead keys or delete their rows/blocks; the
  template NOTE is a two-line deletion.
- docs-02's remaining mass (public git history) is decreed, gated, and
  disclosed — nothing further for this eye to do; it stays Jeremy's call.
- r1 self-correction: docs-11's evidence did not reproduce (memory/ was
  never git-tracked); logged as obsolete rather than resolved to keep the
  record honest about the ~30-50% adversarial hallucination rate.
