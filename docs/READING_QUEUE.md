---
status: living
---

# Reading Queue — docs awaiting Jeremy

Rows here render to the **Reading** tab on the viz server
(`https://maro.feifdom.com/reading.html`). Each row links to the
GitHub-rendered copy on `main` — nothing is self-hosted (Jeremy
2026-07-28: "better just as a link to the github-pushed artifact so we
don't reinvent the wheel"). A row is only useful once its doc has landed
— land first, then queue.

**The page refreshes when this file lands** (`scripts/land.sh`, from the
landed blob) and whenever the runs index is written. Before 2026-08-03 it
was the second trigger only, so a row added in a quiet stretch could sit
invisible for days — that's what Jeremy hit. The page renders the Queue
section only; its header links back here for the Done history.

Add a row when a landed doc needs Jeremy's eyes for a decision — not for
every record. When it's read/decided, MOVE the row to Done (don't delete
it; the queue's history is part of the decision record).

## Queue

| Added | Doc | Why / decision needed |
|---|---|---|
| 2026-08-21 | [Scaling-agent-systems audit (link-farm lead #2; closes your `751e2dea` dispatch)](https://github.com/slycrel/maro-orchestration/blob/main/research/2026-08-21-scaling-agent-systems-audit.md) | **Rewritten 2026-08-22 after your pushback — you were right that the first version took the paper at face value.** What the paper actually gates: putting multiple agents on ONE task (same-milestone fan-out) — that's where the 4.4–17.2× error amplification and coordination overhead live, and we should keep not doing it. What it does NOT gate: your milestone-parallelism idea — distinct work items, near-zero agent-to-agent chatter — which is the paper's *winning* regime (+80.8% on decomposable work). And "our logs show only solo runs" was a design artifact, not evidence: `Milestone` has no dependency field (the schema can't say "B doesn't need A"), `run_mission` is hard-serialized with a drain lock, and `run_parallel_loops` was built (max 3 goals, phase-1 isolation done) and never wired to a caller. So sequential was never a decision — it's a default nobody challenged. **The actual question for you:** promote milestone-DAG parallelism into the actionable stack? The build shape is small and uses parts we already have: `decompose_mission` additionally emits `depends_on` edges → ready milestones run concurrently via `run_parallel_loops`, one agent per milestone, validation gates unchanged, A/B vs sequential before it's a default. Details + honest costs (store contention, lesson-transfer during overlap) in the BACKLOG entry. | — default ON (BACKLOG_DONE § Path-token rewriting)](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG_DONE.md) | Built on your call to stop deferring it. One choice to confirm and one action to schedule. **The choice:** import now rewrites the source install's roots inside extracted text files by DEFAULT (`--no-rewrite-paths` opts out), rather than being an opt-in flag. Reasoning: an import that leaves 44,941 dead paths in place produces a workspace that reads as working and isn't, and an opt-in rewrite is deferral with extra steps. It is loud (prints the mapping), fully recorded (`path-rewrite.json` names every file touched, custody stamps `transformed`), and non-destructive — the archive is untouched, so re-importing with the flag reproduces the old behavior byte for byte. Say the word if you'd rather it were opt-in. **The action:** `~/maro-box-export-v3.tar.gz` predates the export-side `repo_root` record, so importing it maps the workspace and `~/.maro` but leaves the 9,242 occurrences naming the checkout. A fresh `maro-export export` from the box picks up the third root — worth doing before the next box→Mac transfer, not urgent otherwise. |
| 2026-08-11 | [MH #8 scoping — semantic retrieval is near-dead (BACKLOG MH entry)](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG.md) | Measured on the live corpus: memory_quality's fair lane (paraphrase queries) scores **hit@1 2–6% on both adapters** vs 46–80% on the lexical lane — a run asking memory in its own words essentially never gets the stored item. Eval itself is zero-token/~50s, so wiring it as a periodic trend row is cheap. Your call, two levels: (1) wire the eval at heartbeat/GC cadence (small build, includes regenerating the paraphrase cache); (2) whether 2–6% semantic hit@1 bumps the memory-as-module bake-off (MILESTONES arc -1) up the queue — the incumbents lose on exactly the axis a 3rd-party store brings. |
| 2026-08-11 | [Contested 14a scope conclusion (COMPOUND_THINKING §14g)](https://github.com/slycrel/maro-orchestration/blob/main/docs/COMPOUND_THINKING_DESIGN.md) | The one §10-remnant still waiting on you, deferred 2026-08-11 to "when I'm fresher": panel round 2 (both model families) contested the settled §14a conclusion — transferability may follow structural invariance across regimes rather than the method-vs-world category, and "global methodology" may be an n=40 single-family artifact. Your stated prior going in: the answer likely wants probabilistic/fuzzy treatment, not an exact category ("my gut says 'uh… yes?'") — which is closer to what the contest actually claims than the boiled-down summary suggested. Work-depth (§14h) and build threads resolved/deferred in the 2026-08-11 pass. **Update 2026-08-15:** this no longer has to be settled by argument. Slices 1–3 shipped the instrument that decides it — `camera_readout --portability` now cross-tabs earned portability by mint-time method/world stamp, and scope was deliberately kept OUT of ranking precisely because the category has no discriminating evidence yet. Two facts from pre-ship probing worth having before you re-read: on the existing corpus — measured with an offline post-hoc labeller, not the shipped field, which was empty on every live row — every *resolvable* lesson at the ≥3-verdicted-foreign-citation bar is method-scope (only 5 world-scope lessons carry any foreign citation at all), and the stamp is labeller-dependent (~81% method on the production lane vs ~44% on hosted-free, same runs) — which is itself evidence for your "probabilistic, not exact category" prior. The cross-tab reads empty today (all cited lessons predate the stamp) and fills as new mints accrue citations; BACKLOG carries the re-read trigger and a kill criterion for the categorical axis. |

## Done

| Added | Doc | Resolved |
|---|---|---|
| 2026-08-21 | [Edge-review r2 — expansion rendered-inert; A/B gate blocked on render budget](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-21-edge-review-r2.md) | 2026-08-21 — Jeremy CALLED it same day, option (a): *"by now you know my position on arbitrary character limits; Unless there is very good reason, 600 chars is kind of silly low."* Provenance check found none — the 600 dated to April's 9e3d46e7 with zero rationale. recall.py override removed (inject's 1200 default is the one budget, pinned + signature-read by the replay). Gate UNBLOCKED: 22/500 rendered recalls now change (was 0/500); denominator accruing. |
| 2026-08-20 | [Cheap-tier step execution — re-test deferred (BACKLOG § Per-step cheap tiering)](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG.md) | 2026-08-20 — Jeremy CALLED it same day, deferred not dropped: *"let's revisit during an optimization pass in the future… no need to keep that bookmark front and center, that can live in the later."* Direction recorded for when it comes up: **A/B it, gather data, and let maro itself decide what models to use** — ideally learning the choice rather than us hand-tuning a policy table, *"though that might be too complicated."* His read on the original call: *"IIRC we settled on mid model + low thinking to keep it 'cheap', but possible we didn't test it as thoroughly as we should have… at the time it seemed heavy for something we could throw a little spend at."* So the MID-floor decree stands and is no longer treated as an open question; the under-tested part is whether mid+low-thinking was ever properly measured. |
| 2026-08-13 | [Container verbs fix (a) — bake maro into the executor image? (BACKLOG § Container verb parity)](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG.md) | 2026-08-13 — Jeremy CALLED it same morning: "keys should be injected into the container with ENV values at spin-up time, not hosted directly... Agree we shouldn't bake them into the container, host values stay stored and maintained on the host" (backup lane: read-only mounted config dir — noted, not built). SHIPPED same day: image r3 bakes the maro verbs (never keys), `hosted_free_container_env()` injects keys via the docker client's env with bare `-e NAME` flags (values never in argv), consent rides `MARO_HOSTED_FREE_ENABLED` as carrier only (host config wins). Live-smoked end-to-end in-container. Details in BACKLOG § Container verb parity. |
| 2026-08-09 | [World-facts slices 2+3 shipped](https://github.com/slycrel/maro-orchestration/blob/main/docs/WORLD_FACTS_DESIGN.md) | 2026-08-11 — Jeremy re-called all three §7 items, arc CLOSED: (1) unproven facts stay out of stores ("essentially just an assumption") but MAY be prompted in "as long as they're honest about what they are and aren't" → hypotheses now render in a labeled "Unconfirmed guesses" section (was: full prompt quarantine); planner-seeded never-land call stands; (2) emission as slice 3 re-confirmed fine; (3) caps CALLED shallow "2-3 per run for the moment" → landing caps 5/3 → 3/2 — he framed cap depth as part of the bigger how-long-should-we-work tuning question, deferred into the §10 discussion (next up per his same message). |
| 2026-08-05 | [BACKLOG § LT-4 batch results](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG.md) | 2026-08-09 — Jeremy closed the row ("I did mean to remove the reading list with the LT-4 mention"). Batch record stands in BACKLOG: 12/12 PASS, warm = −49% on byte-identical arms, artifacts/continuity are the carriers; dead-drop direction DECIDED 2026-08-06 (promotion-side ingest); all follow-ups closed 2026-08-09 (priors re-anchored, container-gap audit recorded evidence-gated, finding-10 re-diagnosed + prompt-seam mitigation). |
| 2026-08-08 | [Adversarial review: Δ-demotion + session-fork](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-08-delta-demotion-session-fork-adversarial-review.md) | 2026-08-08 — fix pass landed same day (c9cc864); the one open decision (GC re-mint erasing demote stamps) CALLED: gentle re-mint policy — tombstone survives GC, re-mints circulate while gathering data, 3rd re-mint forces full-set re-measurement (decision dcf8eab8). Noted-not-built variants: strict stamp-rides, experimental both-ways-viewable. |
| 2026-08-08 | [Scout wiring — design input](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-08-scout-wiring-design-input.md) | 2026-08-08 — load-bearing call CALLED: scout output is reading material only, never a store write ("gut says we still don't know enough yet... re-open if we need to later", decision 4d562766) — legs 3–4 void, untrusted-git boundary stays closed, ordering question moot. Telemetry + pedigree BACKLOG items stand on their own. |
| 2026-08-07 | [Live-writer census — 3 decision-shaped survivors](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-06-live-writer-census.md) | 2026-08-08 — all three CALLED (decision 1addc859): Phase-60 verification-calibration REMOVED ("makes me a little sad" but agreed — git remembers); inspector gets the evolver's run-finalize cadence lane + periodic deeper-pass rider (Jeremy's recalled resolution was the evolver's — inspector never got it); node promotion judged on age+content. |
| 2026-08-03 | [World-fact plan items design](https://github.com/slycrel/maro-orchestration/blob/main/docs/WORLD_FACTS_DESIGN.md) | 2026-08-08 — §7 all CALLED (decision f8f8d440): hypothesis quarantine at injection mirrors the provenance pattern; planner FACT: emission stays slice 3; cap sizes = build-time tuning. Jeremy open to discussion as testing/review rounds surface edges. |
| 2026-08-03 | [BACKLOG § LeAct — 2026-08-03 amendment](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG.md) | 2026-08-08 — moved to Done: both asks in the row were already CALLED (Δ-gate first 2026-08-06, decision 623eb056; seed-reader A/B ran + acted same day). Row was pure record since. |
| 2026-08-08 | [Δ-gate census round 2 + demotion shipped](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-06-delta-gate-validation.md) | 2026-08-08 — read same day; every decision in the row resolved in-row: 3 negatives stamped ("go ahead and stamp now"), promote floor RESOLVED BY DATA (sweep positives = winner's curse, sign-flipped on full set), retest 2 = no stamps for the flipped pair (run-to-run volatility finding; two-agreeing-full-set-runs standard held). Follow-on noise-floor calibration (temperature question) launched same day. |
| 2026-08-07 | [Δ-gate validation + routes census](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-06-delta-gate-validation.md) | 2026-08-08 — Jeremy read both, keep/adjust CALLED (GOAL_BRAIN decision + journal f7335288): gate stays ("very happy the gate is working"); negative-Δ demotion sanctioned GATED on census round 2 agreeing; Δ decreed surface-scoped (his catch: "other contexts might end up promoting on the same data"); haiku/sonnet-low replay spend cleared standing; calculation variables = adjustable priors. Round 2 launched same session (retest ×3 + stratified ×9 + rule ×2, checkpointed). |
| 2026-08-01 | [Run teachings design](https://github.com/slycrel/maro-orchestration/blob/main/docs/RUN_TEACHINGS_DESIGN.md) | 2026-08-02 — all sections now answered: §4b/§4a/§4d RATIFIED earlier same day (probe-gated first injection); §4c-expanded ANSWERED — terrain at its own surface, separate call to start (maybe parallel; pre/post-planner tension named in the doc), self-teachings caveat-grade injectable as single-win seed with NO solely time-based expiration. New ask folded in → BACKLOG: planner non-action world-fact types (anecdotal vs hypothesis). Decision block in §4c-expanded. |
| 2026-08-02 | [Playbook surprise read](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-08-02-playbook-surprise-read.md) | 2026-08-02 — Jeremy reacted to all flagged entries same day; disposition applied in full (live playbook + repo seed rewritten). Guidance-form decree minted ("usually, do this" — priors not requirements); P2 step-cap removed; truncation root cause fixed (bare [:200] at evolver append sites); frozen alarms/signals retired with mechanism gaps → BACKLOG. Reactions + disposition table recorded in the doc. |
| 2026-07-31 | [Lesson-corpus surprise read](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-07-31-lesson-corpus-surprise-read.md) | 2026-08-02 — Jeremy: remove from the list. Chunk 1's outputs both SHIPPED (retirement-by-contradiction + what-not-how mint-form pass); chunk 2's F1–F8 family verdicts waived — nothing blocks on them now that the tail families ride decay + the contest verb organically. The F1–F8 section stays in the doc if ever wanted. |
| 2026-08-01 | [BACKLOG § Fail-open judge-error edges](https://github.com/slycrel/maro-orchestration/blob/main/BACKLOG.md) | 2026-08-01 — Jeremy: "let's fix the promote validation." Adapter wired same session; validation harness live for the first time, SKILL_PROMOTED events stamp `validation: passed/unjudged/skipped`. |
| 2026-07-28 | [Telegram-runs review](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-07-28-telegram-runs-review.md) | 2026-07-29 — forked session adjudicated all dispositions (Jeremy granted direction/extraction/priority); provenance gate SHIPPED, db37d525+9d6b63fe quarantined, envelope specced (docs/DISPATCH_ENVELOPE.md), maro-dispatch skill 0.2.0 installed. "Already recovered" finding corrected: true on mini2, false-in-effect here — no artifact channel at the dispatch boundary. |
| 2026-07-28 | docs/history/2026-07-28-thread-census.md | 2026-07-28 — split ratified (cap 7), drift batch adjudicated (all four re-parked falsifiably), free slot = closure-check unification |
| 2026-07-28 | [M1 vs box workflow contrast](https://github.com/slycrel/maro-orchestration/blob/main/docs/history/2026-07-28-m1-vs-box-workflow-contrast.md) | 2026-07-29 — **CLOSED, item archived.** All four questions answered (§7) + the residual machine question (§8). Load-bearing claim SPLIT: diff-vs-run observation confirmed, merge-gate remedy rejected ("no master/slave between boxes"); observe-only thresholds rejected as too heavy. Spine corrected to two machines / three kinds of evidence. Produced four new items incl. the newly-decreed out-of-the-box invariant. |
