---
status: living
---

# Config Defaults — what they are, why, and what flips them

Every config key the code reads, its hardcoded default, the reasoning behind
that default, and what changes if you flip it. Written for clean-room
discovery: someone standing up Maro for general orchestration use should be
able to read this file and know which switches exist, which are safe to
touch, and which encode a governance decision.

Resolution order everywhere: **env var > `~/.maro/workspace/config.yml` >
`~/.maro/config.yml` > the hardcoded default listed here** (see
`src/config.py`). The hardcoded default is what a brand-new install runs.

**A second, separate lane** (SF-5/docs-04): `user/CONFIG.md` — flat
`key: value`, hand-parsed by `src/handle.py:_load_user_config`, *not* YAML
and *not* covered by the resolution order above (resolves
workspace-overlay-first via `config.user_file()` since 2026-07-10). It carries run
defaults: `yolo` (also `MARO_YOLO` env), `default_model_tier`,
`research_step_model`, `max_steps`, `always_skeptic`, `ralph_verify`,
`quality_gate`, `quality_gate_action`, `notify_on_complete`, `mcp_servers`
(heartbeat). The census below structurally cannot see this lane (it ASTs
`config.get` aliases only). Full key table + defaults:
[user/README.md](../user/README.md). The prompt-injected user docs
(GOALS/CONTEXT/SIGNALS) resolve workspace-overlay-first via
`config.user_file()` — see the same README.

**The pattern behind the defaults** (worth internalizing before flipping
anything): *capability defaults ON when it only adds internal evidence or
quality; OFF when it self-modifies, acts outward, spends money, or persists
beyond the run.* Every exception below is dated and traced to a decision.

Census enforced by `tests/test_defaults_doc.py`, both directions (reverse
lane = swarm-review chunk 8's enforcement pin): a key read in `src/` but
missing here fails the suite, AND a key documented here that nothing in
`src/` reads fails the suite (`test_every_documented_key_has_a_reader` —
wrapper reads and f-string-constructed keys are resolved by AST shape, so
there is no hand-maintained exemption list). This table can't silently rot
in either direction.

---

## Memory & knowledge

| key | default | why / flip effect |
|---|---|---|
| `memory.worker_slice` | `True` | **ON since 2026-07-08 (Jeremy's flip)** on the §7 A/B verdict — 16 runs pooled, every measure favored injection or tied (closure 8/8 vs 7/8, tokens-in −29% median); record: `history/2026-07-08-worker-slice-ab.md`. Workers get top-5 recalled lessons + parent goal-brain, capped 1200 chars (~300 tokens). Flip OFF → worker prompts revert byte-identical to pre-slice; lose measured closure/token gains. |
| `memory.age_stamps` | `False` | **Time-blindness first slice (BACKLOG vehicle, shipped 2026-07-15):** surfaces timestamps that already exist into prompts. ON = (1) injected lesson/recall items get an age suffix rendered from their stored timestamp — "(learned 2026-02-14 — 5 months ago)" — at the three injection seams (memory_bridge worker slice via `stamp_items_with_age`, the `recall()` loop slice that feeds decompose, `memory.inject_lessons_for_task`); (2) step context gains one elapsed-time line ("Previous step finished 42 minutes ago.") when the wall-clock gap since the previous step is ≥10 min (`_MATERIAL_STEP_GAP_SECONDS`, a module constant in loop_execute.py — deliberately not a config key), delivered through the contribution ledger. The elapsed-time line is **in-process only** (monotonic capture; cross-process resume gaps are out of scope here — hook (b) territory), and a gap immediately before a parallel batch is not surfaced. Time lines are recomputed at each delivery, never replayed by the re-arm paths (`ContributionLedger.drop_source`). OFF for fresh installs per the no-silent-change decree; flag off — or timestamps absent — renders prompts byte-identical (test-enforced, same pattern as the `memory.worker_slice` off-path). Flip ON → injections that actually stamped mark their events (`WORKER_SLICE_INJECTED` / `RECALL_PERFORMED` grow `age_stamped: true`) so the A/B rides the existing captains-log; default OFF — the flip is a per-box config decision. Hooks (b) full elapsed-time model and (c) time-aware recall *ranking* are NOT this flag — ranking/relevance formulas are untouched. |
| `memory.consolidation_enabled` | `True` | Knowledge-web consolidation is internal hygiene (merge/decay of crystallized knowledge); no outward effect. Flip OFF → web grows unbounded, retrieval quality decays over weeks. |
| `memory.step_learning_enabled` | `True` | Per-step learning (2026-07-27): when a run's outcome fails the learnability gate (`is_learnable_outcome`), one extra LLM call extracts 0-3 PROVISIONAL method lessons from steps that individually verified (status `done` + verify confidence `strong`), capped at 8 steps. Provisional lessons enter at score `0.6 + 0.3·novelty` (under the confirmed 1.0 floor), are excluded from every injection surface (`query_lessons`, `inject_tiered_lessons`, memory-bridge worker slice), and never promote to LONG — until a confirmed-context re-record of the same lesson clears the flag (promote-on-evidence); decay archives the unconfirmed rest in ~1 week. Learn more, inject conservatively. Idempotent per run via the `step_lesson_count` stamp on the outcomes row. Flip OFF → failure-shaped runs revert to run-level extraction only; no extra LLM spend, verified-step method evidence is tossed with the run. |
| `memory.consolidation_interval_hours` | `CONSOLIDATION_INTERVAL_HOURS` (knowledge_web.py) | Tuning knob. Lower = fresher web, more LLM spend. |
| `playbook.curation_enabled` | `True` | Playbook curation rides the consolidation dream cycle (chunk 2, 2026-07-21): deterministic dedup always; LLM compression only past the size gate. Every rewrite archives the prior version to `playbook_history/` first. Flip OFF → the playbook only accretes; the 2026-07-16 duplicate-spam class can return (append-time dedup still guards exact re-appends). |
| `playbook.curation_min_chars` | `4000` | Size gate for the LLM compression pass (a CHEAP curation-class call). Below it, only the free dedup runs — no LLM spend on a healthy-sized playbook. Lower = more compress calls; raise to effectively disable compression while keeping dedup. |
| `playbook.alarm_ttl_days` | `14` | How long a playbook ALARM survives without being re-read (2026-08-04). An alarm is an entry that reports a live check — calibration miscalibration, quality drift — rather than a durable insight; it carries its check's key and the date of its last reading, a later reading replaces it in place, and curation drops it once no scan has re-asserted it inside this window. The scanners emit nothing when a condition clears, so silence is the only available "resolved" signal. Raise it if a check runs less often than the drop window (an alarm would expire between legitimate firings and re-appear); lower it to clear stale readings faster. Durable entries (no key) are never expired by this. |
| `knowledge.rule_staleness_days` | `30` | Lens rules older than this get flagged stale rather than trusted. Shorter = more re-verification churn; longer = stale rules steer decisions. |
| `knowledge.novelty_term_enabled` | `True` | Chunk-6 novelty term (2026-07-22): a newly recorded tiered lesson's initial score is `1.0 + 0.3 * novelty`, where novelty = 1 − max text-similarity vs the store measured during the existing dedup scans (zero extra cost). Counteracts the reinforce-the-familiar bias — repeat lessons earn +0.3 reinforcement while novel one-offs died in ~7 days of decay; the boost buys a novel lesson ~2 extra days above the GC line to get tested, and decay still disposes of wrong guesses. Reinforcement never lowers a boosted score (`min(max(1.0, score), score + 0.3)`); promotion is unaffected (sessions_validated still gates). Novelty is measured and stored on the row either way — the flag kills the score boost only, so tabulation keeps its denominator. Flip OFF → initial scores revert to the classic 1.0. |
| `knowledge.provenance_gate_enabled` | `True` | Lesson-mint provenance gate (2026-07-29, `src/lesson_provenance.py`): at the storage choke points (`record_tiered_lesson`, `_store_lesson`) every minted lesson is classified by deterministic regexes as outcome-derived or **prompt-derived** — a lesson whose subject is *what the prompt says* (prompt-authority phrasing, obedience generalization, anti-escalation-scaffolding echo). Prompt-derived lessons record with `minted_from="prompt"`: quarantined from every injection surface (`query_lessons`, `inject_tiered_lessons`, recall flat top-up, graveyard) and from LONG promotion, entered at the provisional 0.6 score so decay disposes of them, kept on disk and marked `[QUARANTINED]` in readouts. A prompt-derived re-record never reinforces an existing lesson (instruction text must not move persistent state); an outcome-derived confirming re-record clears the flag. Answers the db37d525 contamination (a Poe dispatch prompt's "do NOT escalate" scaffolding minted a lesson that recall injected into an unrelated run). Flip OFF → minting reverts to classification-blind; existing `minted_from` stamps keep excluding (the flag gates classification, not the quarantine surfaces — flip is for a misfiring classifier, not for re-arming known-bad rows). |
| `knowledge.contradiction_adjudication_enabled` | `True` | Chunk-4 contradiction wiring (2026-07-21): at skill-maintenance cadence — `run_skill_maintenance` runs at EVERY loop finalize plus evolver cycles, so this is per-run spend when candidates are pending, same as the refight scan beside it — up to 3 pending CONTRADICTION_CANDIDATE events (full-trust failed run that was injected with rules/lessons) get a capped LLM verdict with per-artifact attribution; only an exact "yes" naming specific cited ids calls `contradict_pattern` on those, which is what makes the contested→refight lifecycle reachable at all. Candidates are rare by construction (FULL-trust failures with citations), and a non-blocking cycle lock stops concurrent finalizes from double-judging. Flip OFF → candidates accumulate append-only but are never judged; rules can no longer become contested from run failures (manual `contradict_pattern` calls still work). |
| `knowledge.effect_promotion_enabled` | `True` | Δ-gate effect route killswitch (2026-08-06, `delta_replay.py` + `knowledge_web.promote_lesson_by_effect`): a MEDIUM lesson whose replay-measured Δ (action-match with vs without the lesson on oracle decision calls — runs judged `goal_achieved=True`) clears the bar promotes to LONG on effect, tenure unmet. Instrument validated against pre-registered predictions on the LT-1 arms before wiring (known-effective Δ=+0.59, known-inert Δ=−0.06, rule Δ=−0.15; `docs/history/2026-08-06-delta-gate-validation.md`). ON by default because the route only acts on evidence an operator deliberately produced — measurement is CLI-driven spend (`python3 -m delta_replay`), nothing ambient calls it; the same provisional/quarantined/contested boundary guards as tenure promotion apply. Flip OFF → measured evidence stops moving tiers; the census CLI still reports what WOULD promote. Also gates Δ-as-confirmation (§5 cut B, 2026-08-09, `knowledge_web.confirm_lesson_by_delta`): a PROVISIONAL row clearing the same bars gets its flag cleared (stays MEDIUM, `route="effect-confirm"`) instead of promotion — the retention path for trace-minted lessons (thinkback mints enter provisional with `minted_by="thinkback"`; select via `delta_replay --origin`). |
| `knowledge.effect_promotion_min_delta` | `0.30` (`EFFECT_PROMOTE_MIN_DELTA`) | Minimum measured Δ for the effect route. Set between the validation's inert band (±0.10) and its known-effective specimen (+0.59). Lower = more promotions on weaker evidence; the jackknife-dominance guard (spread < Δ) and reason-stratum requirement still apply. |
| `knowledge.effect_promotion_min_calls` | `6` (`EFFECT_PROMOTE_MIN_CALLS`) | Minimum oracle decision calls behind a Δ measurement before it can promote — below this the per-call jackknife is too coarse to rule out single-call dominance. Raise as the oracle corpus grows. Shared floor: the demotion route reads this same key. |
| `knowledge.effect_demotion_enabled` | `True` | Δ-gate demotion route killswitch (2026-08-08, `knowledge_web.demote_lesson_by_effect`, sanctioned by Jeremy's conditional grant after census round-2 retest re-measured all three round-1 negatives with the same sign). A MEDIUM lesson whose replay-measured Δ clears the demote bar (Δ ≤ −0.05, jackknife < \|Δ\|, ≥ min_calls, reason stratum) gets a `route="effect-demote"` stamp on `delta_evidence`: excluded from `inject_tiered_lessons` and blocked from the tenure route to LONG. Surface-scoped by decree — no score mutation, no deletion, no flat-ledger or `query_lessons` change (decision-replay Δ licenses demotion from decision injection only). Measurement replaces measurement: a later replay clearing the promote bar overwrites the stamp. ON by default (same ambient-spend argument as promotion — only CLI-driven measurement feeds it). Flip OFF → measured negatives stop demoting; the census CLI still reports what WOULD demote. Also gates re-mint recognition (2026-08-08, decision dcf8eab8): a fresh mint matching a GC'd demoted lineage in the archive gets a `route="remint-watch"` stamp — circulates normally (no exclusion, no tenure block; gentle policy), strike `REMINT_PATTERN_STRIKES` (3) emits `LESSON_REMINT_PATTERN` and queues a forced re-measurement (`delta_replay --remint-pending`). |
| `knowledge.effect_demotion_max_delta` | `-0.05` (`EFFECT_DEMOTE_MAX_DELTA`) | Maximum (most positive) measured Δ that still demotes. Set at the edge of the validation's inert band: the census round-1/round-2 stable negatives (−0.137/−0.059/−0.078) all clear it, while the known-inert specimen (−0.06, jackknife 0.09) is stopped by the jackknife-dominance guard. Raise toward 0 = more demotions on weaker evidence. |
| `knowledge.effect_inert_enabled` | `True` | Competence-redundancy decay v1 killswitch (2026-08-13, `knowledge_web.inert_lesson_by_effect` — the LeAct sequence's last step, Jeremy's 2026-08-12 go). A lesson whose replay-measured Δ is a PRECISE null (\|Δ\| ≤ 0.02, jackknife ≤ 0.02, ≥ min_calls, reason stratum, 0 replay errors) gets `route="effect-inert"`: excluded from `inject_tiered_lessons` (its decision-injection slot goes to a lesson that measurably does something) but — unlike effect-demote — tenure is NOT blocked: inert is redundant, not harmful, and the injection exclusion is route-based/tier-agnostic so it survives a tenure promotion. Same surface-scoping as demotion (no score/deletion/flat-ledger/`query_lessons` changes) and measurement-replaces-measurement (a later promote/demote-qualifying replay overwrites). Distinct from `route="measured"` (a noisy/subset null that clears no bar and keeps circulating). ON by default (only CLI-driven measurement feeds it: `delta_replay --inert`); the two-agreeing-full-set-runs acting standard is operator/census discipline, as with demotion. Flip OFF → inert measurements stop freeing slots; the census still reports each row's raw Δ/spread (there is no separate would-stamp field — the operator reads eligibility off the numbers). |
| `knowledge.effect_inert_max_abs_delta` | `0.02` (`EFFECT_INERT_MAX_ABS_DELTA`) | Widest \|Δ\| that still counts as inert — single-call resolution on the 51-call oracle set (±1/51 ≈ 0.0196). The census sweep's exact-0.000 rows (ranks 2/27/50) are the candidate class. Raise = more slots freed on looser nulls. |
| `knowledge.effect_inert_max_spread` | `0.02` (`EFFECT_INERT_MAX_SPREAD`) | Maximum jackknife spread for an inert stamp. A zero claim can't use the demote route's spread<\|Δ\| dominance test (unsatisfiable at Δ≈0), so precision substitutes: the measurement itself must be tight enough that no single call would move it materially. |
| `quality_gate.second_family_check` | `True` | Chunk-5a stack-don't-substitute (2026-07-21): on a paid-gate Pass-1 PASS, one hosted-free second-family call (Groq llama / Gemini flash-lite) judges the same payload; agree/dissent/undecided/no-verdict lands as a QUALITY_GATE_SECOND_FAMILY captain's-log row plus `verdict.second_family`. Flag-only — dissent NEVER changes the gate verdict or triggers escalation (the removed local rung substituted its verdict for the paid one; this one only accumulates the A/B agreement evidence that could later earn it authority). Inert unless `validate.hosted_free.enabled` is on with a configured key (that consent gate owns data egress; this key only governs the extra gate call). Documented expectation: modest lift — all 4 measured gate false-passes were narration-vs-evidence, already caught by the deterministic claim probes. Flip OFF → gate behavior byte-identical to pre-chunk-5a; no agreement data accrues. |
| `quality_gate.cross_ref_research` | `True` | Chunk-5b (2026-07-22): research-shaped goals (workers.infer_worker_type == research) get the gate's cross-ref pass on the hosted-free tier — fresh-context-per-claim second-source fact check (the genuinely decorrelated design, zero organic uses before this). Flag-only lane: disputes land as a QUALITY_GATE_CROSS_REF row (`acted=false`) and `verdict.cross_ref`, never flip the verdict — the weaker family doesn't overrule the paid gate. Inert unless `validate.hosted_free.enabled` is on (consent gate owns egress; this key only governs the research-lane call). The `strict:` paid lane is NOT governed by this key and keeps its acting behavior. Flip OFF → research goals get no cross-ref; strict: unchanged. |
| `quality_gate.closure_overrule` | `True` | Verdict reconciliation (2026-07-29, ead11c12-nimble-shore): a gate ESCALATE on a "done" run is overruled when the run's closure verdict defends it — judged, complete, checks_run > 0, confidence ≥ 0.7 (the same bar closure needs to *demote* a done; defend and overturn are symmetric). Closure runs executed probes over the delivered artifacts while the gate judges a ~2.5KB excerpt — the observed failure was the excerpt-fed gate escalating a 5/5@0.88 run into a power re-run that blew the cost budget and buried the delivered memo. The dissent lands as a QUALITY_GATE_OVERRULED captain's-log row (both sides recorded) instead of a re-run. No closure verdict / unjudged / conf < 0.7 → escalate behaves exactly as before. Flip OFF → gate escalate ignores closure again (pre-2026-07-29 behavior). |
| `camera.frame_log_enabled` | `True` | Chunk-A camera frames (2026-07-31, round-3 panel disposition): the recall loop-slice lesson-selection fork logs itself FORWARD — candidate sets with raw ranker scores + normalized shares (explicitly not probabilities), chosen/rendered IDs, substrate sizes — one JSON line per recall appended to the run's `source/camera_frames.jsonl` (`src/camera_log.py`; consumer: `python3 -m camera_readout`). Selection behavior is untouched (the wider n=10 fetch is instrumentation; selection stays first-3, test-pinned). Never raises; no run dir = frame dropped, never orphaned. Flip OFF → runs stop accruing frames; the camera readout's axis/verdict/overdraw joins go dark for new runs. |
| `recall.dispatch_guard` | `True` | Stops a failing goal from being re-dispatched in a tight loop (the Apr "stale mission shortcircuit" bug class). Flip OFF only when deliberately stress-testing retry behavior. |
| `recall.dispatch_inject` | `True` | Injects the dispatch recall slice (prior outcomes/lessons for similar goals) into goal handling. Read-side of the memory system at the dispatch seam. Flip OFF → dispatches lose the fuzzy/semantic history lane (the 2026-05-17 "same goal ran 25× in 35min" class returns for non-identical goals); since 2026-08-10 the deterministic exact-match rerun brief (`rerun.brief` below) is a separate lane with its own switch — full dispatch amnesia requires flipping both. |
| `recall.guard_attempts` / `recall.guard_window_minutes` | `3` / `60` | The guard's budget: 3 dispatches per hour per goal. Widen for legitimately retry-heavy workloads. |
| `rerun.brief` | `True` | **Re-run identity (2026-08-10; BACKLOG 2026-08-09, Jeremy: "a re-run should know it's a re-run, with prior art")** — at intake, a normalized exact-text match over `memory/handle_inputs.jsonl` yields the COMPLETE list of prior attempts at this goal (zero LLM spend, `src/rerun_identity.py`), each verdict read WITH STANDING from run metadata (closure / operator_restamp / contested — a corrected or disputed record can never silently read as plain failure). Rendered as a provenance-labeled brief and injected at two seams: the dispatch navigator's input (its prior for "verbatim re-dispatch, latest attempt succeeded" must differ from "fails repeatedly" — specimen: the 6b14e413 Act-1 escalation at conf 0.95 quoting a since-restamped verdict recall had served unattributed) and the AGENDA run context beside dispatch recall (step 1 becomes "build on <prior deliverable>" instead of project archaeology). Exact-match only by decision; fuzzy/paraphrase is a separate later call. Flip OFF → dispatches rely on recall's top-k fragments alone for goal history. |
| `project.risk_mint` | `True` | **Risk minting (2026-08-10, Jeremy ruling: loop-discovered risks/unknowns DO belong in project RISKS.md).** After closure judges, `loop_finalize._mint_run_risks_to_project` appends the run's closure-verdict gaps (≤3, from the run dir's `closure_verdicts.jsonl`) and any scope-parse-failure sentinel to the project's RISKS.md via `append_risk`. Mechanical, zero LLM spend, idempotent per loop (skips when RISKS.md already names the loop_id); loops held by an unresolved verdict audit never mint. Flip OFF → run-discovered risks live only in run-dir records; RISKS.md returns to orch-lane-only writes. |
| `pack.export_denylist` | `[]` | Extra emails/handles `maro-pack export` redacts to `[REDACTED]` via `secret_scrub.scrub_identifiers()`, on top of auto-derived ones (`$EMAIL`/`$GIT_AUTHOR_EMAIL`/`$GIT_COMMITTER_EMAIL`, `git config user.email`) — PORTABLE_LEARNING_DESIGN.md §4: the deny-list is assembled from config + environment, never hardcoded. Empty by default; a fresh install redacts only what it can auto-derive. This is one of two scrub guarantees behind a pack (the other is secret-shaped `scrub()`) — neither claims mechanical anonymization; the mandatory human review gate (`REVIEW.md` / `maro-pack seal`) is the real backstop. |

## Governance & safety (decision-carrying — don't flip casually)

| key | default | why / flip effect |
|---|---|---|
| `workers.allow_main_push` | `False` | Workers may push work branches, never the default branch (Session-40 governance; enforced by `scripts/hooks/pre-push` + `MARO_WORKER_RUN` set at the spawn seam in llm.py; liveness tripwired in `tests/test_git_guard.py` after the hooksPath incident, 2026-07-08). Flip ON → every worker subprocess gets `MARO_ALLOW_MAIN_PUSH=1`; only sane on a throwaway repo. Per-goal override: export `MARO_ALLOW_MAIN_PUSH=1` under explicit goal authorization instead. |
| `constraints.allow_persistence_install` | `False` | Workers cannot install cron/systemd/launchd persistence ("good system citizen": no self-rearming loops; off switches stay off — the Apr rogue-heartbeat lesson). Flip ON only for a goal whose explicit deliverable is scheduled automation, and review the diff. |
| `heartbeat.autonomy` | `False` | The heartbeat may observe but not self-generate work. This box killed its always-on heartbeat in Apr 2026 after a 48h unattended token burn. Flip ON = the system wakes itself up and spends; requires a working budget gate and someone watching the first week. On an autonomy-mode evolver cadence, the same finite background pass first attempts up to 3 `audit_repair_required` records; verdict replay is local and the cheap adapter is built lazily only when a repaired row still needs deferred lesson/knowledge extraction. Poison/missing records leave quarantine visible but stop automatic retry (other failures cap at five) until a targeted manual retry. No separate daemon or timer. |
| `heartbeat.backlog_every` | `DEFAULT_BACKLOG_EVERY` (heartbeat.py) | How often an autonomy-enabled heartbeat looks at the backlog. Irrelevant while `heartbeat.autonomy` is off. |
| `sheriff.dormant_days` | `DORMANT_DAYS_DEFAULT` = `14.0` (sheriff.py) | Projects with no file activity for this many days classify `dormant` instead of `stuck`/`warning`, so tier-2 LLM diagnosis and tier-3 escalation never spend on them (BACKLOG #21: first real heartbeat ticks diagnosed dead test projects among ~230 accumulated — the June-21 cron-tokenburn class; a recurring hook would re-buy those diagnoses every interval). Set `0` to disable. Hygiene sibling: `maro sheriff archive [--days N] --apply` moves dormant projects to `projects/_archive/` — manual only, dry-run by default. |
| `skills.lite_promotion` | `True` | **ON by decree (Rider A, Jeremy 2026-07-09):** "we want things promoted to skills that the local orchestration can pick up and use while waiting for user review... degraded the same as regular skills." A deliberate exception to the self-modification-defaults-OFF pattern: skill-shaped .md artifacts from successful runs auto-copy into the workspace skills overlay (`tier: skills-lite`, fail-closed dangerous-pattern scan, never overwrites an existing name) plus a companion provisional Skill in skills.jsonl so normal stats/decay/circuit-breaker degradation applies; a tripped companion quarantines the .md to `skills/_quarantine/` (`run_curation.promote_skills_lite` / `degrade_skills_lite`). Human review gates only ship-set/repo graduation. Zero LLM spend — static scan + file copy at run finalize. Flip OFF → Maro-built skills sit in run artifacts until a human moves them (pre-Rider-A behavior); the quarantine sweep also stops. |
| `evolver.auto_enqueue_signals` | `False` | The evolver proposes self-improvements; it does not enqueue them for execution by itself. Human (or explicit config) stays in the self-modification loop. Flip ON = closed-loop self-modification — the north star, but only after the verify→learn loop is trusted. |
| `evolver.auto_apply` | `False` | Whether the evolver may auto-apply `new_guardrail` suggestions (guardrails can permanently block execution paths). Default OFF = `held_for_review`; apply held items via `maro evolver --apply <id>` (a manual apply always goes through — the review IS the gate). Replaces the pre-2026-07-10 `environment != production` inference: there is no dev/prod split anymore, the system always runs production semantics, so self-modification gates are explicit knobs, not environment guesses. Env override `MARO_AUTO_APPLY_GUARDRAILS=1/0` wins over config. Injection guard and the skill_pattern test gate run regardless of this knob and regardless of manual. |
| `evolver.run_cadence` | `0` | Run-cadence trigger for the evolver meta-cycle (2026-07-09 supervision decision): every N-th non-dry-run finalization fires `run_evolver()`, riding the run's LLM adapter — no daemon or timer ("app, not systemic"), and no runs means no evolver, which is the correct no-op. `0` = off; fresh installs never make unrequested meta-cycle LLM calls. Set e.g. `10` → one meta-cycle per 10 completed runs (counter in `memory/evolver_cadence.json`; failures never break finalization). |
| `inspector.run_cadence` | `0` | Run-cadence trigger for the quality inspector (decision 1addc859, 2026-08-08 — the evolver's lane extended to the inspector, whose threshold cluster had live inputs but no live caller since the heartbeat daemon stopped; live-writer census survivor 2). Every N-th non-dry-run finalization fires `run_inspector()` on the run's adapter, writing `inspection-log.jsonl` — which brings the three friction readers (conductor, heartbeat summary, quality gate) live for the first time. `0` = off (fresh installs make no unrequested inspector LLM calls); counter in `memory/inspector_cadence.json`; failures never break finalization. This box runs `10` (workspace config). |
| `inspector.deep_every` | `5` | The periodic larger-cleanup rider on the inspector cadence (Jeremy 2026-08-08: "kick off a parallel processing run periodically... for a larger cleanup"): every N-th cadence *firing* widens the pass from 50 to `DEEP_PASS_LIMIT` (200) outcomes — same finalize hook, lower frequency, no daemon. `0` = never deep. |
| `evolver.verify_cadence_verdicts` | `True` | **VERIFY_LEARN_ARC V2** (`docs/VERIFY_LEARN_ARC.md` §3): at each evolver cadence, `verify_applied_suggestions()` renders a behavioral verdict on every applied-but-unverified change by comparing its class-neutral stuck-rate before/after apply (trust-filtered per §4), then acts: **confirmed** → stamp + feed confidence calibration; **degraded + self-applied** → auto-revert (the system cleans up its own mess); **degraded + human-applied** → surfaced to the review queue, never auto-reverted (authority asymmetry). No LLM calls, rides the existing cadence hook (no daemon), and only ever *reverts* changes the system itself applied — so unlike `auto_apply`, this is a safety mechanism, hence default ON. Gated in practice by `run_cadence` (0 = no cadence = never runs). Flip OFF → reverts to warn-only; degraded auto-applied changes stay live. |
| `evolver.verify_min_post_apply` | `10` | Minimum trust-counted outcomes recorded *after* a change was applied before V2 renders a terminal confirmed/degraded verdict (the "10 finalizations post-apply" window, §3). Below this the change is inconclusive and re-examined next cadence. Also the recent-baseline size sampled *before* apply. |
| `evolver.verify_max_extensions` | `3` | How many consecutive cadence passes may render *inconclusive* (too little post-apply data, or no directional movement) before a change is parked `verify_verdict="unverifiable"` (terminal). An honest unverifiable beats an eternal pending (§3). |
| `evolver.verify_delta_threshold` | `0.05` | Stuck-rate movement (fraction) a change must show, in the expected direction, to count as confirmed (down) or degraded (up). Movement smaller than this is flat → inconclusive. Matches the neutral band `scan_evolver_impact`/`maro evolver impact` already used. |
| `evolver.verify_use_class_signal` | `True` | **VERIFY_LEARN_ARC V3** (`docs/VERIFY_LEARN_ARC.md` §3): when an applied change declares a `failure_class_rate` `expected_signal` (graduation templates do — all 9), `verify_applied_suggestions()` verdicts it on that class's rate over timestamped-diagnosis windows instead of the class-neutral stuck-rate. This is what lets a graduation verdict *resolve*: a single failure class barely moves the global stuck-rate, so on that metric graduation rows only ever park `unverifiable`. Self-falls-back to the stuck-rate whenever the class windows are thin (a sparse class parks honestly rather than verdicting off noise), so it is never worse than V2. Each diagnosis's date comes from its `recorded_at` stamp (V3, go-forward) or an events-log join on `loop_id` (`_loop_ts_index`, ~99% coverage) — so the class path is live on the full historical ledger, not dormant. Flip OFF → forces the class-neutral stuck-rate for all rows (the V2 metric) — for A/B or debugging only. |
| `validate.input_provenance` / `validate.output_provenance` / `validate.result_provenance` | `True` | Anti-hallucination arc: claims need positive evidence (FS-diff artifact checks, claim probes). Flip OFF → "done" reverts to trust-the-model; the 2026-04 fabrication incidents are why these exist. Cost: a little latency per validation. |
| `validate.write_fence` | `True` | **ON since 2026-07-09 (1.0 posture)**: a done step whose tool transcript shows an out-of-fence WRITE demotes to blocked (positive evidence only; `/tmp` and goal-declared paths stay writable via `FENCE_EXTENDED`). This box ran it enabled since 2026-07-04 with no false-positive incident; a fresh install shouldn't ship with enforcement off. Flip OFF → out-of-fence writes are logged (`SCAVENGE_DETECTED`) but the step stays done. |
| `validate.scavenge_detect` | `True` | Always-on detection layer under the fence: scans real tool transcripts for out-of-fence reads/writes and logs `SCAVENGE_DETECTED`. Zero-cost, evidence-gathering only. Flip OFF → the fence has no eyes; only do this to debug the detector itself. |
| `validate.artifact_check` | `True` | Fabrication FS-diff guard (2026-06-26 arc): a step claiming a named artifact that doesn't exist on disk demotes done→blocked. Positive-evidence only (absence of a *named* file, never an empty diff). Flip OFF → reverts to trusting worker claims; the eab4b2d incident class returns. |
| `validate.memory_provenance` | `True` | Memory twin of the result-provenance guard (2026-08-02 incident): a run given a standing convention mid-run closed a step with *"durable feedback memory already persisted and complete — no write required"* and wrote nothing; the next run started blank. When a result narration claims in completed tense to have persisted named content to durable memory, `provenance.memory_provenance_unverified` looks for a matching row across the lesson ledgers, tiered stores, knowledge nodes, decision journal, and rules — no LLM, no freshness gate ("already persisted" is a legitimate claim, and duplicates reinforce rather than append). **Advisory: it marks `memory_provenance_unverified` in run metadata and never moves the verdict** — the lookup half is exact but the claim half is a regex over prose, and that half is where every false demotion of the file guard came from. Claims that don't name what they persisted are skipped, not accused. Flip OFF → unbacked memory-write claims go unrecorded again; the failure stays invisible because its cost is paid by a later run, not this one.  Also records a WEAKER, non-accusing category: a step asserting durable memory needs no write while naming nothing checkable (`memory_claims_unverifiable`) — the founding incident's exact wording, which the accusing check correctly skips because there is nothing to look up. Recorded so the shape stops being invisible; never an accusation and never a verdict input. |
| `closure.verdict_audit` | `False` | **Second opinion on negative verdicts (2026-08-09; specimens 18773dfa and the 2738d9c0 class; hardened same day by a 3-lens adversarial review)** — when closure is about to record not-achieved with NO mechanical failure evidence (zero cleanly-failed checks, at least one clean pass), one extra LLM call gets the goal, the verdict's own reasoning, the check outcomes, and workspace-contained, injection-scanned excerpts of the artifact files the checks referenced (quoted as explicitly-untrusted data), and is asked whether the evidence supports not-achieved. A typed disagreement (exact JSON boolean, confidence ≥ 0.6) cancels a pending **Signal-1** downgrade only — Signals 2/3 and the diagnosis gap rest on structured runtime declarations, where the missing behavioral probe IS the finding, so audit disagreement there records `disputed` instead of cancelling. A judge-asserted False gets ONE retry with the objection attached (locally error-bounded: a retry failure preserves the original verdict); a retry that flips to achieved is re-run through the deterministic downgrade detectors before it stands; a retry that maintains False stands, stamped `verdict_audit.disputed` — handle.py routes those loops into the contested learning holdout and stamps `goal_verdict_contested(_by="verdict_audit")` in run metadata (2026-08-02 decree: anecdote, not a vote). Default OFF per the fresh-installs-conservative pattern (a pass that can reverse verdicts and spends adapter calls is opt-in); **this box opts in via workspace config**. Cost when on: one ~256-token call per evidence-free negative verdict, plus at most one retry — negatives of that shape are rare (single digits in this box's corpus). |
| `closure.pass_audit` | `False` | **Pass-side audit — MH #1 Specification Gaming v1 (2026-08-10, model—grader edge)** — the mirror of `closure.verdict_audit`'s false-negative lane: an ACHIEVED verdict resting entirely on static/preflight checks (nothing executed the deliverable) is exactly the shape an executor can game by writing artifacts that assert success, so it gets one adversarial refutation call holding the goal, the verdict's reasoning, the check outcomes, and the same workspace-contained injection-scanned artifact excerpts (shared evidence lane, `_audit_artifact_evidence`). Detection degrades trust, never flips: a typed refutation (exact JSON boolean, confidence ≥ 0.6, positive evidence the pass is hollow — coverage gaps alone don't refute) caps verdict confidence at 0.6 (below the 0.7 `VERDICT_CONFIDENCE_FLOOR`, so learning stops treating the achievement as full trust) and stamps `verdict_audit.pass_audit` with `mh_class: specification_gaming_candidate` for corpus analysis. Skips when any behavioral probe ran, when a downgrade is pending (the negative-audit lane owns those), and on dry runs. Default OFF per the fresh-installs-conservative pattern; **this box opts in via workspace config**. Cost when on: one ~256-token call per all-static positive verdict. Honest scope: v1 is detection + trust-degradation — prevention (harness-side execution receipts closure can check against) is the deeper open item on the MH entry. |
| `validate.write_fence_allow` | `[]` | Extra paths workers may write outside the bounded workspace. Empty = fence as designed (goal-declared paths still widen it per-run via `FENCE_EXTENDED`). Additions are standing holes — prefer goal-declared paths. |
| `validate.auto_verify` | `True` | Default the ralph verify loop ON whenever the hosted-free validator tier is usable (verification is then free). Was local-model-conditional until the local tier's removal (2026-07-21 decree); the reader is `loop_post_step._free_auto_ralph_enabled`. Flip OFF = ralph verify runs only when explicitly requested (`ralph_verify=True` or a `ralph:`/`verify:` goal prefix), even with hosted-free configured. With hosted-free unconfigured/inert the flag is moot — no free tier, no auto-enable. |
| `validate.hosted_free.enabled` | `False` | Explicit consent gate for sending validation payloads to Groq/Gemini. API-key presence proves authentication, not consent to third-party data egress. Flip ON to make hosted-free the free validation rung (the local-model backup tier was removed 2026-07-21 by decree — UNDECIDED/no-verdict now escalates straight to paid); absent keys still make the tier inert. This box opts in. |
| `validate.hosted_free.max_latency_ms` | `20000` | Per-request transport timeout and subsequent per-provider latency-breaker threshold for hosted-free validation. Network calls are hard-bounded by this value. `0` disables the cap and restores the generic 120-second transport timeout. |
| `budget.per_run_usd` | absent = auto: `max(10.0, 4 × p90)` | **Data-driven since 2026-07-29 (Jeremy decree: the breaker is a circuit-breaker for pathological runs, not a cost optimizer)** — when the key is ABSENT the cap scales with what success actually costs: `max($10 floor, 4 × p90 of successful-run costs)` from run cards (`metrics.successful_run_cost_p90`, ~15-min cache; thin history < 8 samples → floor). Floor raised $5→$10 the same day: a sanctioned power-tier step alone costs ~$3 (escalated re-run 3a4e2692 died at $3.00 against a $2.40 cap while holding a finished memo). An explicit number stays a static cap (history never inflates it); `0` (or null) stays the uncapped opt-out. Capped-by-default posture unchanged since 2026-07-09. Enforced by the mid-loop hard stop at budget + 20% slush. |
| `budget.warn_usd` | absent = auto: `max(2.50, p90)` | **Early-warn line (2026-07-29, same decree)** — "this run has left typical territory," advisory only, warn-once. Absent = `max($2.50, p90 of successful runs)`; explicit number = static; `0` (or null) = off. Clamped to 80% of the breaker if set at/above it. Crossing it logs dollars internally and emits ONE `effort_note` on the conversation channel in **effort language, no dollars** (2026-07-17 spend-UX decree). Callers that bypass the budget gate keep the legacy warn at 80% of budget. |
| `budget.daily_usd` | `25.0` | **Capped since 2026-07-09 (1.0 posture)** — cross-run gate on `metrics.spend_today()`; refusal = stuck + escalation notify, resets UTC midnight. Stops a substrate burning through runs one under-cap loop at a time. Set `0` (or null) to opt out. |
| `budget.transparency_usd` | `2.0` | Runs costing more than this get surfaced explicitly in curation/reporting rather than folded into totals. Lower = noisier reports; higher = spend hides. |
| `budget.extension_ladder` | `True` | **ON by default (Jeremy decree 2026-08-02: "out of budget becomes a notification the first time with a budget extension… and the same for the second extension, with a pause-ask-user the third time in the same run… keep the budget hooks, but no big stoppage")** — an out-of-budget breach (token or cost, one shared counter per run) with work remaining grants one run's worth of extra budget (the ORIGINAL cap, added on top) and continues; the third breach ends the loop `interrupted` with the typed `budget-decision` pause so the user decides (continue / trim scope / accept partial). Each extension logs a `BUDGET_EXTENDED` row (dollars internal) and emits an `effort_note` in effort language (2026-07-15 spend-UX decree); cost-side extensions lift the runaway-meter ceiling proportionally so the extension isn't dead on arrival at the adapter seam. An explicit `0` budget still means "spend nothing" — never extended. Amends the enforcement edge only; caps stay circuit-breakers (2026-07-29), the runaway circuit and daily gate are untouched. Flip OFF → restore the pre-2026-08-02 hard stops (out-of-budget stop verdict). |
| `budget.runaway_multiplier` | `1.5` | Mid-step runaway circuit (BACKLOG #23e, 2026-07-11): once run spend crosses `multiplier × cost_budget`, the adapter seam refuses further calls pre-call (zero cost) instead of waiting for the step boundary — run 8a20665f's step 9 alone burned $2.04 past a $2.40 ceiling. Deliberately runaway-only (Jeremy: don't churn-kill legit long steps; "cost isn't the end-all-be-all"): sits ABOVE the between-step hard stop (budget + 20% slush), armed for the execute phase only (never finalize/closure/quality-gate — demotion-bug lesson 8f8344a). The stream-side half (`llm._build_stream_cost_probe`, #23e residual) kills a call already in flight once meter spend + the in-flight estimate crosses the same ceiling. Set `0` (or null) to disable. |
| `llm.subprocess.fresh_input_ceiling_tokens` | `300000` | Mid-step token brake (tire-runs deferred item, 2026-07-27): a stream-side probe on every agentic `claude -p` call sums uncached ingest (input + cache_creation tokens, deduped per API message id — cache READS deliberately excluded) and kills the ONE runaway subprocess past the ceiling. Surfaces as error_class `token_runaway`: the STEP blocks, the run continues with its remaining budget — unlike `budget_runaway`, which stops the run. 300K is measured, not reasoned (2026-07-27, 123 cost-metered steps on this box): healthy fresh ingest is cost-bounded at p95 ≈ 259K with the pathology band at 337-478K — run 3's tire step (2.14M recorded, $1.21, one call) sits in that band. Don't calibrate against raw run-log `tokens_in`: it includes cache reads and (pre-fix) a per-content-block double-count. Token-denominated and always-on, so it guards even runs with no cost meter armed. Env `MARO_SUBPROCESS_FRESH_INPUT_CEILING` wins; `0` disables. |
| `llm.subprocess.weighted_input_ceiling_tokens` | `600000` | Ceiling #2 of the same brake: weighted ingest = fresh + cache_read/10 (input-side cost in fresh-token equivalents). Closes the transcript-amplification hole the fresh ceiling can't see (2026-07-27 adversarial review): 250K fresh then twenty re-reads of that cached transcript ≈ 5M input tokens while total_fresh never moves — but 750K weighted. 600K ≈ 2.3× the measured healthy p95 bound and above every step observed on this box (max 478K), so it fires only on the amplification shape; the fresh ceiling owns the grinder band. Same kill path, error_class `token_runaway` with `trigger="weighted"`. Env `MARO_SUBPROCESS_WEIGHTED_INPUT_CEILING` wins; `0` disables that ceiling only. |
| `llm.subprocess.bash_max_output_chars` | `24000` | Per-tool-call ingest cap handed to the claude CLI as `BASH_MAX_OUTPUT_LENGTH` on agentic subprocess calls: the CLI itself persists oversized Bash output to a file and puts only a cap-sized slice (plus the file path) in the model's context — a giant `curl`/`cat`/multi-MB JSON response cannot inject more than ~6K tokens per **Bash** call (verified live on CLI 2.1.220, 2026-07-27). Honest scope (2026-07-27 review, accepted): this governs the Bash tool only — the Read tool has no equivalent knob, so `curl -o file` + Read bypasses it; a wholesale Read of a large capture lands as fresh ingest and the accumulation ceilings above are the backstop. 24000 chars keeps the fetch CLI's own capped output (~20.3K chars) passing through untruncated. Also forwarded through the container lane's env passthrough. An operator's own exported `BASH_MAX_OUTPUT_LENGTH` is respected (maro doesn't overwrite it); env `MARO_BASH_MAX_OUTPUT_CHARS` wins over both; `0` disables (the CLI's own default persistence threshold still applies). |

## Executor / sandboxing (containerized executor arc, 2026-07-12)

The agentic executor lane — worker steps running `claude -p ...
--dangerously-skip-permissions` with real tools — can optionally run inside a
docker container for filesystem/network isolation (design:
`CONTAINER_EXECUTOR_DESIGN.md`; ratified worth-the-effort by Jeremy
2026-07-12, GOAL_BRAIN Decisions). Docker is **never a hard Maro
requirement** — with `executor.container` off, worker steps run on the host
under the write-fence exactly as before. C1 ships image + auth + doctor rows;
C2 wires the actual wrap.

| key | default | why / flip effect |
|---|---|---|
| `executor.read_query` | `True` | **`maro-read` sub-query verb — REPL step 2 v1 (2026-08-11, gate opened by A/B-3).** A CLI the executor's inner session shells out to: a hosted-free model (Groq/Gemini validation ladder) reads bounded slices of named local files OUT-OF-BAND and returns only the answer + an honesty receipt, so large-file bytes never enter the step conversation (per-turn-resend is the measured cost driver). Default ON because the verb is inert without the REAL gates: it refuses unless `validate.hosted_free.enabled` is opted in (file content leaving the box is egress — a key is authentication, not consent) and provider keys exist; it adds NO new spend paths (rides exactly the hosted-free ladder configuration already consented to — free-tier models in the shipped config; pointing the ladder at a billed model prices this verb and the validators alike). Bounded: ≤8 files, ~48KB total slices (every emitted char counted), ≤700-token answer enforced locally too, ≤4MB local scan per file, no-tools single-shot (cannot recurse). Sensitive paths (.ssh/.aws/.gnupg/secrets/.env/keys) refused — accident guard, not containment. Injection-flagged answers are WITHHELD (caller falls back to direct reads). NOTE: not baked into the executor-container image — bake maro-read + maro-fetch before any `executor.container` flip (BACKLOG, C4-BOX). Flip OFF → the CLI reports itself disabled and the repl_reading protocol's direct-read path applies unchanged. |
| `executor.container` | `off` | `off` / `on` / `require`. OFF everywhere until runtime-box burn-in (C4); the flip — the fresh-install default especially — is **Jeremy's call after burn-in evidence**, not a code change. `on` = wrap executor calls in a container, degrading to host/fence-only with one loud warning per run if docker is unavailable (SF-6: the difference must be visible). `require` = refuse executor calls outright when docker is unavailable instead of degrading. An unrecognized value fails safe to `off` (host), and doctor reports the raw value. Off = byte-identical to pre-container execution. |
| `executor.container_image` | `maro-executor:<CLAUDE_CLI_VERSION>-r<IMAGE_REVISION>` (`container_exec.py`) | The baked executor image tag. The CLI pin answers "which claude"; the `-r<N>` contents revision answers "which image", so one tag never names two different builds (design §3 auditability). Bump `IMAGE_REVISION` for any Dockerfile change that doesn't move the CLI pin — a digest census in `tests/test_container_exec.py` fails if you forget. Build/re-pin via `maro-bootstrap container-setup`. Override to point at a custom-built image. |
| `executor.container_network` | `bridge` | Docker network for executor containers. `bridge` (egress on) in v1 — workers legitimately fetch web content and the CLI needs api.anthropic.com; narrowing to an egress-allowlist proxy is a deliberate later follow-on. `none` exists from day one for offline-shaped goals. |
| `executor.container_extra_mounts` | `[]` | Additional **read-only** reference mounts (reference data, repo checkouts) beyond the fence-derived rw mounts. Empty = only what the fence computes is mounted. Additions are standing read grants — add from evidence, same posture as `validate.write_fence_allow`. |
| `executor.container_run_scratch` | `True` | Bind a per-run host scratch dir (`<run_dir>/scratch`, on the workspace subtree) at the container's `/tmp` so scratch written to `/tmp` survives across a run's steps — each `--rm` step otherwise gets a fresh ephemeral `/tmp` and silently loses it (C4-BOX burn-in follow-up, 2026-07-15). Per-run, not per-step (a step-named dir would still lose it across steps). Retained with the run per the data-retention decree. Flip `false` to restore ephemeral per-step `/tmp` if the persistent dir ever causes stale-state (leftover locks/sockets). |
| `executor.introspection_access` | `True` | **ON by default, inert unless `executor.container` is on** (decree 2026-07-18: "Install in the container only for the runs that need access"). When `intent.classify()` flags a goal introspection-shaped (`introspects_self` — asks about Maro's own runs/behavior/source), that run's containerized executor steps get read-only provisioning: the workspace `runs/` dir + the maro source dir, plus `MARO_INTROSPECTION`/`MARO_INTROSPECTION_RUNS`/`PYTHONPATH` env markers (`container_exec.introspection_provision()`). Same prevents-doomed-runs argument as `now_lane.live_data_routing`: without it, a self-diagnostic run spends its whole budget proving only that it can't see the records it was asked to diagnose (brisk-saffron: 2.8M tokens / 28min). The workspace root, `memory/`, config, and secrets stay hard-excluded (`build_mount_map`'s forbidden filter still applies — defense in depth); non-introspective runs are byte-identical to before. Flip `false` to keep even introspection-shaped runs blind-isolated. |

## Navigator / dispatch-class rollout (staged cutover, 2026-06→07)

| key | default | why / flip effect |
|---|---|---|
| `navigator.shadow_dispatch` / `navigator.shadow_blocked_step` | `False` | Shadow mode: navigator decides but decisions are only logged, not executed. Ship-dark pattern — 23 live NAVIGATOR_DECIDED events, 14/14 execute agreement before any act flag moved. |
| `navigator.shadow_tiers` | `['cheap']` | Shadow runs on the cheap model tier; it produces no user-visible output, so paying for a big model buys nothing until decisions act. |
| `navigator.act_dispatch` | `True` | Navigator dispatch decisions *execute* (escalate-only via `navigator.act_moves`). **Default ON since 2026-07-08 (Jeremy's flip)** after live evidence on this box since 2026-06-21: 14/14 execute agreement, zero bad escalates — and escalate is safe-by-construction (defers to a human, cannot assert a wrong resolution). Cost of ON: one cheap-tier model call per autonomous dispatch (the decide call fires even with shadowing off). Fail-open by construction: synthesized idunno-chain escalates (chain exhausted — includes adapter outages) never act, so an unreachable navigator cannot stop the line. Flip to `false` for shadow-only decisions with zero act authority. |
| `navigator.act_blocked_step` | `False` | Same idea at the blocked-step recovery point. Stays OFF by default: evidence base is thinner (24 rows, recoverable-class n=5) and a wrong escalate here aborts a run mid-flight rather than pre-dispatch. This box opted in 2026-07-03 via workspace config; re-verify against organic usage before defaulting on. |
| `navigator.act_moves` | `['escalate']` | Per-move allowlist once act is on: escalate-only first because a wrong escalate wastes tokens, a wrong close buries work. Add `'close'` only after organic close-agreement evidence. |
| `navigator.act_confidence_floor` | `0.9` | Below this confidence the navigator defers to the legacy path even with act on. Lower = more navigator authority, more wrong-move risk. |
| `navigator.tiers` | `DEFAULT_TIERS` (navigator_prompt.py) | Model ladder for live navigator calls. |
| `navigator.shadow_planning_depth` | `False` | Planning-depth shadow (thread-arch #5, MILESTONES 1.5, decided 2026-07-09). Rides the SAME `decide()` call `navigator.shadow_dispatch` already makes at live dispatch — no extra model call, just a longer system prompt (the depth addendum) and one more envelope field (`planning_depth`: `plan` / `one-shot` / `thin-plan` / `spawn-sub-goal`). Off = byte-identical prompt to today. On = dispatch-only (deliberately not wired into `shadow_blocked_step_live`, per the decided design); records `pipeline_actual.depth_equivalent` (constant `"plan"` today, since the default autonomous dispatch pipeline is always the full-plan pipeline) for `analyze_planning_depth_agreement()` / `--agreement` to compare against. Decide-only, changes no real behavior — same ship-dark posture as the dispatch-class shadow above. |
| `navigator.adjudicate_divergences` | `False` | **VERIFY_LEARN_ARC V4** (`docs/VERIFY_LEARN_ARC.md` §5): at evolver cadence, LLM-adjudicate un-adjudicated navigator/pipeline divergences (navigator_right / pipeline_right / both_defensible), appending append-only `NAVIGATOR_ADJUDICATED` rows the `--agreement` table reads for its `adjudicated` breakdown and V5 clusters into navigator lessons. **OFF for strangers** — it is an LLM spend per cadence (like `scope_generation`, no silent cost); this box opts in via `~/.maro/workspace/config.yml`. Nothing is acted on/reverted — evidence only. Manual: `python3 -m navigator_shadow --adjudicate [--dry-run]`. |
| `navigator.adjudicate_max_per_cycle` | `5` | Cap on divergences adjudicated per cadence pass — bounds the per-cycle LLM spend. Unjudged divergences carry to the next pass (append-only, idempotent — already-adjudicated rows are skipped). |
| `navigator.adjudicate_tier` | `"cheap"` | Model tier for the adjudication call. Adjudication is a bounded judgment on a small context; the cheap tier is sufficient (same posture as `navigator.shadow_tiers`). |
| `navigator.lesson_inject` | `False` | **VERIFY_LEARN_ARC V5** (`docs/VERIFY_LEARN_ARC.md` §5): inject navigator-scoped lessons (crystallized from ≥3 same-shape `navigator_right` adjudications) into `decide()`'s recall context — the same injection seam worker slices use (`memory.worker_slice`). **OFF by default** — A/B flag, shadow-comparable: flip ON to feed the navigator its own adjudicated wins, measure move-agreement lift against OFF. No new model call (rides the existing `decide()` request); the lessons ride the `recall_block`. The A/B readout is `PYTHONPATH=src python3 -m navigator_shadow --agreement` (by-lesson-inject table, swarm-review chunk 7) — decisions group by whether `lessons_injected` was stamped. |

## Lanes & execution

| key | default | why / flip effect |
|---|---|---|
| `terrain.enabled` | `True` | **ON by default (RUN_TEACHINGS §5b, Jeremy 2026-08-02: "a goal short term memory issue")**: run-scoped terrain memory — hosts that HARD-block a step (403/401/429/451, cloudflare challenge, **AWS WAF challenge** — `x-amzn-waf-action: challenge|captcha|block`, which arrives as an HTTP *202* with an empty body and is invisible to every status-code signal; added 2026-08-02 after a live run hit it on hup.harvard.edu — quota exhausted, paywall, robots disallow) are recorded from the real tool transcript and rendered as one `terrain` contribution into every later step's prompt, so step N+1 stops rediscovering step N's block. Deterministic, no LLM call, observation-only (never changes step status or routing). Measured waste it closes: the cold chlorination run re-attempted the same blocked archives across ~6 steps (~$15 of a $25 run). Transient failures (5xx, timeouts, resets) are deliberately EXCLUDED — a false "blocked" belief would suppress a source that actually works. Flip OFF → the accumulator stops recording and the contribution never renders (prompts byte-identical to pre-§5b). Facts die with the run; promotion to a durable terrain teaching is a separate, unbuilt chunk. |
| `world_facts.enabled` | `True` | **ON by default (WORLD_FACTS_DESIGN slice 1, §4c ask + §7 decisions 2026-08-06)**: plan-native capture of non-action findings. The executor MAY declare `{kind: anecdotal\|hypothesis, fact, evidence}` entries in the completion tool's `world_facts` field (guidance-form, max 3/step, malformed entries don't consume the cap). Declarations land in a run-scoped ledger on LoopContext that rides the run checkpoint, so resume/replan sees the facts, not just the surviving steps. Anecdotal facts render as one `world_facts` contribution into later steps ("Facts already established THIS RUN", capped 10 rows, same drop/re-render discipline as terrain); **hypothesis-kind facts render in a separately-labeled "Unconfirmed guesses" section, capped 3** (§7.1 as amended by Jeremy 2026-08-11 — prompting them in is fine "as long as they're honest about what they are and aren't"; the label is the honesty, the sections never mix, and the render tells the model never to restate a guess as fact; before this amendment they were fully quarantined from prompts — the self-referential-hedging concern §4c routed around; since slice 2 (2026-08-09) finalize routes them to `observe_pattern` and anecdotal facts to candidate knowledge nodes via `land_facts`, idempotent per run dir, planner-sourced facts excluded; landing caps shallow per Jeremy 2026-08-11 "2-3 per run": 3 anecdotal + 2 hypotheses, strongest first). No LLM classification — the declarer says which kind, and every declaration passes the deterministic `injection_guard` scan before it can ledger (flagged → dropped, fail-closed). Flip OFF → the validation site drops declarations, the render site stops injecting (covers facts restored from a checkpoint written while the flag was on), finalize landing no-ops, and the planner emission teaching goes silent too — so prompts are byte-identical apart from the tool schema/guidance text, which stays. Scope note: the carry guarantee is the sequential lane's — parallel fan-out/DAG runs have no checkpoint/resume machinery at all, so their facts are run-scoped only. |
| `pause.environmental` | `True` | **ON by default (§13e decree, Jeremy 2026-08-02: "one is a pause (out of tokens/error, not budget); one is a conclusion that can be restarted")**: a step failing terminally with an environmental error class (`billing_actionable`/`retry_at` → `no-tokens`; `failover` chain exhausted → `llm-unreachable`) ends the loop `interrupted` with the typed pause stamped, instead of churning through blocked-step retries against a dead backend to `stuck`. Closure skips interrupted runs, so the continuation lane's strict-affirmative resume test routes the follow-up as a same-identity RESUME once the environment heals — this is what makes §13e's reserved `no-tokens`/`llm-unreachable` vocabulary real. Auth failures stay terminal-surfaced (credentials need a human; deliberate vocabulary gap). **The budget half of this row is superseded** (`budget.extension_ladder`, row above): it used to read "the chosen budget ceiling stays a CONCLUSION (out-of-budget stop verdict, restart semantics)", which was true under the 2026-07-29 caps=circuit-breakers decree. Under the extension ladder the first two breaches neither conclude nor pause — they notify and extend — and the third breach PAUSES on `budget-decision` (operator family), so a budget stop is no longer purely a conclusion. `out-of-budget` remains the stop verdict for a run that actually ends on budget; what changed is that reaching a ceiling no longer implies ending. Flip OFF → restore the old churn-to-stuck behavior; pause stamps stop, so exhausted-environment continuations become restarts again. |
| `now_lane.escalate_to_director` | `True` | NOW-lane goals that outgrow a single response escalate to the director instead of silently under-delivering. Flip OFF → NOW stays cheap but complex asks get shallow answers. |
| `now_lane.escalate_on_not_achieved` | `False` | **OFF fresh installs, ON this box (2026-07-11, Jeremy "we should just do this")**: when the NOW self-verdict judges the quick answer not-achieved (non-fulfillment OR non-answer — "goal asked where, answer has no where"), re-route to a full AGENDA run with the failed answer attached as context, instead of returning a recorded failure to a caller nobody reads. The Manti NOW failure (answered from model knowledge with a how-to-search list) is the specimen. OFF by default because one cheap verdict then triggers a full orchestrated run — real spend needs an operator decision (same posture as `scope_generation`). Task-path only by construction (the self-verdict only runs when `origin` is set); `force_lane="now"` always wins. |
| `now_lane.artifact_retry` | `False` | **OFF fresh installs, ON this box (2026-07-29, trio-triage PARTIAL — shallow artifact-seeded retry only)**: when the NOW self-verdict judges the quick answer not-achieved, take ONE more single-shot in the same lane before any agenda escalation — the original ask re-runs seeded with the failed answer (first 1500 chars) and the demotion reason, then the retry is re-judged against the ORIGINAL ask. Complex directives skip the rung (structural failures go straight to escalation); a retry that errors keeps attempt 1's answer. Attempt 1 stays fully recorded (artifact + outcome row); the retry writes its own `-retry` artifact, outcome row, and `NOW_ARTIFACT_RETRY` event. OFF by default for the same no-silent-spend reason as `escalate_on_not_achieved` — an extra LLM call on the strength of one cheap verdict. The 3-arm retry-shape experiment and both-lane matrix are explicitly out of scope (BACKLOG). |
| `now_lane.interactive_self_verdict` | `True` | **ON by default, inert without hosted-free CONSENT — i.e. both a key AND `validate.hosted_free.enabled` (default false; a credential proves authentication, not consent — review F6 correction: keys alone do NOT arm this)** (chunk B, 2026-07-31 — panel round 3: "close the two verdict-blind lanes"): interactive NOW runs (`origin` absent) get a self-verdict too, but the text judge rides the hosted-free ladder ONLY — the paid adapter is never invoked for it, preserving the deliberate keep-raw-speed decision, and hosted-free's consent gate (build returns None without keys or without the egress opt-in) makes the flag inert otherwise, so ON-by-default adds no silent spend. The judge runs on a daemon thread under a hard ~5s wall-clock budget; on breach or judge error the answer returns at raw speed un-judged, with `goal_verdict_source="now_self_verdict_error"` stamped so a broken pipe never masquerades as "unjudged by design". Without hosted-free only the free deterministic provenance guard runs and the row stays honestly unjudged. A different judge family than the player is a feature (correlated-errors finding, same panel). Rows judged this way carry `goal_verdict_source="now_self_verdict_free"`. Flip `false` to restore verdict-blind interactive NOW. |
| `now_lane.live_data_routing` | `True` | **ON for fresh installs too** (`docs/history/2026-07-12-routing-and-probe-synthesis-design.md` Part A, DECISION provisional, 2026-07-12) — deliberately differs from the OFF-by-default posture above. `intent.classify()`'s `needs_live_data` schema field (LLM path) and the heuristic's flipped `current/latest/today's` lexical pattern (no-LLM path) both route a live-data ask to AGENDA before any NOW call is attempted — closes the routing half of the Manti canonical-case failure (`docs/CAPABILITIES.md`), where "gas near Manti, Utah" routed NOW and answered from model knowledge with a how-to-search list. Not gated OFF-by-default like `scope_generation`/`escalate_on_not_achieved` because those re-run a goal after an answer already landed (extra spend on top of a result); this one decides *before* any NOW spend happens, and a wrong-lane doomed NOW call is worse than routing correctly the first time — the AGENDA cost-envelope work (cuts-first, ladder breaker) is the mitigation for the resulting spend, not a reason to misroute. Flip OFF to restore pre-2026-07-12 behavior (both the schema field and the heuristic flip go inert; `current/latest/today's` counts toward NOW again). |
| `closure_restart` | `True` | Status-integrity arc: demoted/failed closures restart rather than lingering "done". Flip OFF → done≠achieved drift returns. |
| `recursion.checkin_first_depth` | `2` | Goal-pass depth at which the first non-blocking recursive-goal check-in fires (`docs/RECURSIVE_CHECKIN_DESIGN.md`; Jeremy decree 2026-07-13). `director.handle_escalation`'s `continue`/`narrow` branches re-enqueue a fresh continuation at `continuation_depth+1` — a chain of sequential distinct goal executions. At `new_depth==2` (starting the 3rd goal pass, "2 goals deep beyond the first") the system fires a progress conversation that explains the plan and how it serves the original ask, and tells the user they can redirect or stop — **but the goal keeps running** (ralph-style optimistic default; this is NOT the `escalate` navigator move, which parks the goal). Lower = check in sooner; higher = let recursion run deeper before the first conversation. Set very high to effectively silence check-ins (the continuation still enqueues either way — this knob never stops the goal). |
| `recursion.checkin_jitter_min` / `recursion.checkin_jitter_max` | `4` / `7` | After the first check-in, the next one fires a jittered `randint(min, max)` goal-passes later (decree: "every 4-7 goals after that ... progress update, interact with a chance to redirect or stop"). Jittered, not a fixed period — same "signals not rule tables" posture as the rest of the system. Widen the window for noisier/quieter cadence; `min` is floored at 1 and a reversed pair is swapped. Purely controls check-in frequency, never whether the goal continues. |
| `scope_generation` | `False` | Phase 65 scope/ResolvedIntent generation (inversion → scope → deliverables, injected into planning + plumbed to closure). OFF for fresh installs: it costs one extra LLM call per AGENDA run — no silent spend for strangers. The runtime box audited in July explicitly opted in with injection live from 2026-07-09 (Purgatorio SF-4 / Jeremy decision 7); **this M1 dev host currently has no user/workspace config, so its effective posture is OFF**. The 2026-04-22 A/B's reliable signal was plan compression (8 vs 15–40 steps); closure-quality benefit remained under-tested. History: the audited runtime box had previously been mis-documented as "PAUSED/dormant" while it ran generation-without-injection since ~April. Deeper Phase 65 design discussion (constraints, enforcement, human gate) still deferred — `CONSTRAINT_ORCHESTRATION_DESIGN.md`. |
| `scope_ab_skip` | `False` | A/B **control-arm** bypass: scope is generated and recorded but NOT injected. The A/B concluded 2026-04-22 (inject won); the flag survives only because `scripts/scope_ab_runner.py` flips it per arm for any future re-run. Leave unset in real configs — setting it silently pays the scope call while discarding the benefit (the exact SF-4 bug). |
| `adaptive_execution` | `False` | Phase 64 Phase A adaptive execution (`ADAPTIVE_EXECUTION_DESIGN.md`): mid-run `director_evaluate` on `verify_failure` (2+ session verify failures) / `step_threshold` (every 5 steps) triggers, plus the stuck-trigger arm — director may continue/adjust/replan/restart/escalate. OFF by default = no extra LLM spend mid-run. (Row said "not started" until 2026-07-16 — stale; the `_ae2` block in `loop_execute.py` has been live code with tests since Phase 64.) |
| `director.evaluate_on_injection` | `False` | §6a decision-point (`SESSION_PROTOCOL_DESIGN.md`): when an operator interrupt is applied at a boundary poll and the loop continues, fire `director_evaluate(trigger="injection")` so continue/adjust/replan stays an explicit decision — the injection text reaches the director via `EvaluationContext.injected_context`. Independent of `adaptive_execution` (its own spend gate). OFF for strangers (paid LLM call per applied interrupt); this box opts in (Jeremy 2026-07-16: "agree, build + enable"). |
| `advisor.stuck_step` | `False` | Consult a power-tier model once at the terminal-stuck boundary — skip / retry-once / abort guidance before the loop gives up (`loop_execute.py` Advisor Pattern). History: this block was **dead code from birth** (`.get()` on StepOutcome dataclasses raised into a catch-all — 2026-07-15 stuck-outcome review), so with `adaptive_execution` off, stuck always went terminal. The 2026-07-16 fix ACTIVATES a paid LLM call on stuck paths, hence the gate: OFF for strangers (no silent spend), this box opts in (Jeremy: "fix + config-gate... turn it on by default for ourselves for actual testing"). Advisor (b)-retry gives the stuck step exactly one more attempt per consult; the stuck-flagged execution is still recorded (continue-exit contract). |
| `curation.answer_synthesis` | `False` | **Delivery-loop arc (Jeremy 2026-07-17: "we ask a question and should get an answer")**: at curation, one cheap `no_tools` call turns the run's top deliverable into `answer_summary` — the direct answer to the goal — which every completion surface (Telegram message, Hermes DM, run card) leads with. OFF for fresh installs: it is an LLM call per completed run (no silent spend, same posture as `scope_generation`); this box opts in via workspace config. When OFF (or on any LLM failure) `synthesize_answer` still writes a deterministic excerpt of the deliverable body (`answer_source: "excerpt"`) — worse prose, same answer-first orientation, zero spend. `locate_deliverables` (mechanical, ungated) feeds it: project files modified during the run window, report-shaped names ranked first, top pick copied into `<run>/artifact/` so the viz server can serve it (`deliverable_link_path`). |
| `notify.viewer_url` | `""` (off) | Base URL of a reachable viz server; when set, completion messages carry a clickable link to the run's deliverable (preferred) or loop report. Empty = no link line — a fresh install has no viz server to point at. Pick a base reachable from where you READ messages, not where the server runs: same-box setups can use `http://localhost:8787`; multi-device, prefer a tailscale IP/MagicDNS name or a real DNS name over a LAN IP (LAN IPs break off-network and can be reassigned by DHCP). |
| `artifacts.auto_prune_days` | `0` (never) | **Retention decree (Jeremy, 2026-07-10):** run data is kept by default — "I'd prefer to have the users choose to archive/delete old runs, rather than have the system decide it's clutter... the result isn't always *just* the outcome, it's also the path that gets you there." Replaces the retired `keep_artifacts` flag, whose default (delete at finalize) was ruled a bug — it destroyed the audit evidence behind a semantic miss (BACKLOG #18, hermes specimen). Set >0 to opt in to pruning per-step `loop-*-step-*.md` files older than N days at finalize (`loop_finalize.cleanup_step_artifacts`); even then the just-finished loop's files are never touched (its verdict is judged after the loop returns). Plan manifests, RESULT/PARTIAL.md, loop logs, and scratchpads are never pruned. |
| `planner.cuts_first` | `False` | Cuts-first planning (Qix-cuts decree, Jeremy 2026-07-10: "cuts off of a rectangle to narrow the field of view in 0-4 ish steps, then do the work inside those boundary lines"). Before decompose commits a plan, `planner.draw_cuts()` makes one LLM call: committed constraints from prior knowledge + 0-2 cheap probes. Probes become the plan's first steps; a `[boundary]` marker step is expanded mid-loop WITH the probe findings in context (`loop_execute` boundary expansion) — the plan for the bounded remainder is drawn after evidence lands, not before. Narrow/medium goals only (staged-pass already narrows wide/deep). OFF for fresh installs: one extra LLM call per goal — same no-silent-spend posture as `scope_generation`. The Manti canonical case (docs/CAPABILITIES.md) is the acceptance test. |
| `executor.session_reuse` | `False` | Opt-in per-boundary Claude Code executor-session prototype. A compatible sequential segment sends the full standalone prompt once, then uses `--resume` with step deltas plus the loop's authoritative audited prior-step state; model/cwd/tool/permission/context changes force fresh, boundary/milestone/replan/interrupt/failure/high-risk-content/turn-cap rotates, and parallel/container/dry-run paths stay stateless. Only Claude's proven zero-turn/zero-token pre-execution missing-session diagnostic falls back once to the full prompt. Clean session ID + compatibility signature ride the checkpoint; an in-flight crash omits them. Default OFF: the fixed-Sonnet A/B showed a 27.9% provider-cost point estimate but no executor speed gain and 5.7% worse end-to-end wall time (`docs/history/2026-07-14-session-reuse-prototype.md`). |
| `executor.session_max_turns` | `6` | Maximum successful Claude CLI calls retained in one executor segment when session reuse is enabled. Reaching the cap persists a clean rotation before the next call; bounds provider-held history even when no semantic boundary fires. |
| `planner.persona` | `None` | The framework orchestrates as the neutral Conductor; personas (e.g. Poe) are opt-in per the Maro rename decree — persona is presentation, not authority. |
| `planner.recon_flavor` | `True` | Recon step flavor (compound-thinking §4/§9.2, graduated from the star contract 2026-08-01). ON = decompose teaches the `[recon: <decision it informs>]` tag (`planner.RECON_FLAVOR_RULES`) so information-buying steps are typed, with the VOI rule ("no decision named → cut it or fold it in") as prompting. The tag rides the step string like `[after:N]`; tagged steps get a map-edit execution contract (`step_exec._RECON_STEP_EXTRA`), the map-change verification question (`verification_agent._VERIFY_RECON_STEP_SYSTEM`) at every ladder tier, and `flavor`/`recon_decision` stamped on the step outcome. Cuts-first probes are tagged deterministically at `planner._cuts_plan` (VOI = the boundary plan they inform) — the cuts lane returns before the taught decompose runs, so prompt-side emission never reaches it (2026-08-01 smokes shipped textbook recon probes untagged); the same switch gates this. Recon steps are also exempt from milestone expansion (loop_execute) for the same reason they skip `_shape_steps`: text rebuilds strand the tag. OFF kills EMISSION only — downstream detection stays unconditional (chunk-6 killswitch precedent: kill the boost, never the measurement), so in-flight markers keep honest verification while no new ones appear. |
| `planner.world_facts` | `True` | World-fact planner emission (WORLD_FACTS_DESIGN slice 3, §4c ask 2026-08-02). ON = decompose teaches `planner.WORLD_FACT_RULES` in the lanes that carry the assembled system prompt (narrow/multi-plan/single-shot); the staged-pass lane is deliberately NOT taught (2026-08-09 review, 3-lens consensus: it replaces the assembled prompt and never receives the injected-context extras, so a staged FACT could only be a model prior wearing a grounding claim): the planner MAY emit `"FACT: ..."` array entries for concrete terrain facts it can point to in the INJECTED context (guidance-form — "most plans need no FACT entries"). Parsed FACT entries never become steps and never count against `max_steps` (`parse_steps` plucks them before truncation, unconditionally — chunk-6 killswitch precedent: this detection stays live with the flag OFF, so a stray FACT line can never execute regardless). Collected facts seed the run ledger as ANECDOTAL with sticky `source="planner"`: they render into step prompts as already-known like any anecdotal fact, but `land_facts()` refuses to write them back to durable stores — they came FROM injected context, and landing them would launder what the stores already say into fresh confidence bumps (the portable-learning provenance-stamp lesson; a step restating a rendered planner fact is laundering too, so provenance never upgrades). Requires `world_facts.enabled` (master switch silences this surface with the rest). Excluded from the cuts JSON prompt like `RECON_FLAVOR_RULES` (output-format teaching is noise there). Flip OFF → the planner is never taught the prefix; no new FACT entries appear. |
| `loop.admission_wait_s` | `0.0` | How long a new run polls a busy project's admission slot before refusing (`refused_busy`). `0` = refuse immediately — on an unattended box a queued run invisibly pins memory and the model lane; NEXT.md is already the queue and the heartbeat retries next tick. Env `MARO_ADMISSION_WAIT_S` wins; `maro-handle --wait N` sets it for interactive use. The slot itself is a per-project flock held for the run's lifetime — kernel-released on any death, so only a *live* run can make another wait. In-process sibling loops (mission feature fan-out, parallel goals) share the slot rather than refuse — the gate excludes other *processes*. |
| `loop.busy_policy` | `"refuse"` | What a run does when the project slot is held by another process. `refuse` = mutual exclusion (complete fix for git/NEXT.md collisions; heartbeat retries next tick). `worktree` = proceed in an isolated git worktree of the project dir and merge back at finalize — merge conflict downgrades the run to `partial` with the work preserved on a named `maro/<loop_id>/run` branch. OFF (`refuse`) since 2026-07-09 (concurrency phase 3b): opt-in until burn-in shows autonomous merge-back behaves; non-git project dirs fall back to `refuse` regardless. Intra-run parallel fan-out steps get worktree isolation unconditionally (no flag) when the fence dir is a git repo. |

## Platform & I/O

| key | default | why / flip effect |
|---|---|---|
| `debug` | `False` | Debug-information switch (Jeremy, 2026-07-10 — replaces the retired dev/prod `environment` split: behavior is always production; only *observability* is switchable). ON = `maro.*` loggers at DEBUG instead of WARNING (`loop_types._configure_logging`). `MARO_LOG_LEVEL` (exact level) and `MARO_DEBUG=1` env overrides win over config. Purely a log-verbosity knob — flipping it never changes what the system does, only what it says. |
| `model.backend_order` | `None` | `None` = built-in resolution order (on this box that lands MODEL_POWER on subprocess `claude -p`, which shares Jeremy's plan quota — accepted trade per budget posture 2026-07-08; API lane deferred to the budget-models phase). Set an ordered list to re-route. |
| `subprocess.session_fork` | `True` (`_SESSION_FORK_DEFAULT`) | Fork-master lane for the `claude -p` backend (2026-08-08, Jeremy's session-fork idea; benchmark at `~/.maro/workspace/output/claude-fork-bench/`): stateless subprocess calls resume a seeded master session with `--fork-session` instead of starting cold. Why it pays: a bare `-p` call re-creates ~12k cache tokens EVERY call — the CLI's per-invocation context is never a byte-stable prefix across processes so it never cache-hits (the old slack-bridge no-cache finding, now with receipts) — while a fork replays the master transcript as a stable prefix (cache_creation ~250 vs ~12k). The saving is a **fixed ~12k tokens/call**, so the benchmark's ~5×-cost/34%-latency figures hold for utility-sized calls and shrink proportionally for large agentic prompts (adversarial-review correction). Masters keyed by (binary, model, no_tools, cwd), TTL 1h (`_FORK_MASTER_TTL_S` — the master transcript freezes CLAUDE.md-era context at seed time), persisted atomically to `<workspace>/state/subprocess_fork_masters.json`; the seed rides `_run_subprocess_safe` with the caller's exact flag set (worker push-guard marker, runaway breakers, identical tool contract — 2026-08-08 review findings 1–3); forks are mutually isolated (fresh session id per call; each transcript starts with the 1-turn seed exchange, and forks write their own session files like every `-p` call — nothing is written back to the MASTER); executor sessions and container calls stay bare; evicted masters fall back bare with owner-checked invalidation, failed seeds are negative-cached 15 min. ON by default (Jeremy same-day: "Let's turn that on by default" — flipped after live smoke + the in-flight Δ retest finished so the substrate never changed mid-measurement); marginal cost of ON ≈ one ~$0.04 seed per shape per hour. Flip OFF → bare cold-start calls, byte-identical to pre-2026-08-08 behavior. |
| `notify.command` | `""` | Empty = external notifications OFF. Notifications run an arbitrary shell command — outward side effect, so opt-in. Events always land in `events.jsonl` regardless. |
| `notify.events` | `DEFAULT_EVENTS` (notify.py) | Which captain's-log event types trigger the command once one is configured. `backend_actionable` (auth/billing/context death with the fix command), `stranded_run` (crashed run with a checkpoint + the `maro resume` command), `resume_refused_busy` (a second resume was immediately refused), and `resume_lock_unavailable` (the admission store itself failed) are default-on — the notify channel may be the only surface an away-from-keyboard user sees. All four also land in the durable escalation file. Opt out by setting an explicit list without them. |
| `notify.timeout_seconds` | `30` | Kill a hung notify command rather than wedge the loop. |
| `telegram.chat_id` / `telegram.chat_ids` | `None` | Unset = Telegram listener refuses to engage; doubles as the allowlist of chats it may answer. Never wildcard this — it's the auth boundary. |
| `captains_log.rotate_mb` / `captains_log.rotate_keep` | `5` / `1000` | Log rotation: size trigger + retained-event floor. |
| `health.probes_enabled` | `True` | Subsystem-liveness probes (`src/system_health.py`) at goal-run finalization — the 2026-07-29 monitoring decree's no-cron cadence. ON: report-only (snapshot at `memory/system_health.json` + `SUBSYSTEM_SILENT`/`SUBSYSTEM_RECOVERED` transition events); reads existing artifacts, no model calls, never blocks the run. OFF stops the snapshot from updating — the SILENT streak clock freezes rather than resets. |
| `record.enabled` | `True` | Byte-level LLM call capture to `<run-dir>/build/calls/` (secret-scrubbed). ON because recorded calls are replay fixtures and mining input — internal evidence, no outward effect. Off (`false` or `MARO_RECORD=0`) trades future replay/mining ability for disk. |
| `report.enabled` | `True` | Per-run HTML report + cross-run static index (run-visibility, 2026-07-08). ON: pure read-side rendering of data already captured — no model calls, no outward effect. `false` stops report generation only; capture is `record.enabled`. |
| `report.debug_snapshots` | `False` | Extra intermediate report snapshots for debugging the report generator itself. OFF: developer-only output, disk noise in every run dir when on. Env override `MARO_REPORT_DEBUG`. |
| `viz.host` | `"127.0.0.1"` | Bind host for the read-only run-visibility HTTP server (`src/viz_server.py`, `scripts/viz-ctl.sh`). Loopback-only default — avoids repeating the archived dashboard's accidental `0.0.0.0` default. Set `"0.0.0.0"` (or a LAN IP) to deliberately reach it from another machine, e.g. a headless runtime box — not internet-facing, opt-in only. |
| `viz.port` | `8787` | Bind port for the same server. |
| `viz.autostart` | `False` | Start the viz server (detached, `viz_server.ensure_running()`) at goal-run entry when nothing is listening on `viz.host:viz.port` — the viewer survives reboots without a system agent because the next run revives it. OFF for strangers: autostart opens a listening socket as a side effect of running a goal, which is an operator decision. This box opts in (Jeremy 2026-07-16). `viz-ctl.sh stop` still works on an autostarted server (shared pidfile + pgrep fallback). |
| `correspondence` | `None` | dev-recall (developer tooling) config block; `None` = defaults. Not part of runtime self-improvement — keep it that way. |
| `file_lock.timeout_s` | `30.0` | How long a writer waits for a ledger's advisory lock before failing closed (`FileLockTimeout`, an OSError). Env `MARO_FILELOCK_TIMEOUT_S` wins. flock is kernel-released on holder death, so only a *live* slow holder can make a waiter hit this; every critical section is a local file rewrite (ms), so 30s is generous. Shorter = faster failure under pathological contention; longer = more patience, more stall. |
| `file_lock.fail_open` | `False` | **OFF since 2026-07-08 (concurrency-hardening arc)** — reverses the original deliberate fail-open (warn + write unlocked after ~5s), because contention is exactly when unlocked writes corrupt the learning ledgers, permanently and silently. Flip ON (or `MARO_FILELOCK_FAIL_OPEN=1`) to restore warn-and-proceed — the operator escape hatch to un-wedge an unattended box without a deploy. |
