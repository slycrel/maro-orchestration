# Goals — operator template

<!--
  WHAT THIS FILE IS
  The operator's top-level and active goals. The planner injects the first
  ~500 characters of this file into every goal-decomposition prompt
  (src/planner.py) so the director can bias toward work that advances
  your objectives.

  WHERE YOUR REAL FILE GOES
  Do NOT put personal goals in this repo copy — it ships with the code.
  Put your real file at:  ~/.maro/workspace/user/GOALS.md
  (more precisely: <workspace_root>/user/GOALS.md). The workspace overlay
  always wins over this shipped template. See user/README.md.

  Keep it short: only the first ~500 chars reach the prompt.
-->

## North Star
<!-- One or two sentences: the long-horizon outcome everything should serve.
     Example:
     Build an autonomous research assistant that compounds knowledge
     across runs and needs less supervision every week. -->

## Active (right now)
<!-- 2-4 bullets: what the system should prioritize this week.
     Example:
     - Stabilize the ingestion pipeline: reliable, replayable, low token burn
     - Get the nightly regression loop producing a trustworthy signal -->

## Medium-term (this month)
<!-- Example:
     - Move from operator-specified missions to self-proposed sub-missions -->

## Values (guide execution priorities)
<!-- Example:
     - Progress over perfection. Ship and iterate.
     - Evidence before adoption (no hype-driven feature adds)
     - Compound: every run should make future runs smarter -->
