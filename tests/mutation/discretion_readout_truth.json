[
  {"name": "loader :: torn line drops the whole archive again (strict read revert)",
   "file": "src/discretion_readout.py",
   "anchor": "        try:\n            lines = store_text(p).splitlines()\n        except OSError:",
   "replacement": "        try:\n            lines = p.read_text(encoding=\"utf-8\").splitlines()\n        except Exception:",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "loader :: tainted-valid line served into the report (launder direction)",
   "file": "src/discretion_readout.py",
   "anchor": "                e = loads_clean(line)",
   "replacement": "                e = json.loads(line)",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "loader :: skipped lines no longer counted",
   "file": "src/discretion_readout.py",
   "anchor": "            except Exception:\n                cov[\"lines_skipped\"] += 1\n                continue",
   "replacement": "            except Exception:\n                continue",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "loader :: substring prefilter becomes the scope rule",
   "file": "src/discretion_readout.py",
   "anchor": "            if isinstance(e, dict) and e.get(\"event_type\") in CONSUMED_EVENT_TYPES:",
   "replacement": "            if isinstance(e, dict):",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "metacog :: evidence-free means fingerprint CHANGED",
   "file": "src/discretion_readout.py",
   "anchor": "        if action == \"retry\" and len(fps) >= 2 and fps[-1] == fps[-2]:",
   "replacement": "        if action == \"retry\" and len(fps) >= 2 and fps[-1] != fps[-2]:",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "metacog :: every action counts as a retry candidate",
   "file": "src/discretion_readout.py",
   "anchor": "        if action == \"retry\" and len(fps) >= 2 and fps[-1] == fps[-2]:",
   "replacement": "        if len(fps) >= 2 and fps[-1] == fps[-2]:",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "reinjection :: every goal counts as a duplicate",
   "file": "src/discretion_readout.py",
   "anchor": "    dupes = {g: n for g, n in goal_counts.items() if n > 1}",
   "replacement": "    dupes = {g: n for g, n in goal_counts.items() if n >= 1}",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "gate :: undecided rows deflate the agreement rate",
   "file": "src/discretion_readout.py",
   "anchor": "    decided = sf_by_decision.get(\"AGREE\", 0) + sf_by_decision.get(\"DISSENT\", 0)",
   "replacement": "    decided = len(sf_rows)",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "gate :: prefix normalization dropped (table reads SECOND_FAMILY_AGREE)",
   "file": "src/discretion_readout.py",
   "anchor": "        d = str((e.get(\"context\") or {}).get(\"decision\") or \"?\").replace(\n            \"SECOND_FAMILY_\", \"\")",
   "replacement": "        d = str((e.get(\"context\") or {}).get(\"decision\") or \"?\")",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "novelty :: pre-chunk-6 rows counted as zero-novelty lessons",
   "file": "src/discretion_readout.py",
   "anchor": "        if \"novelty\" not in c:\n            missing += 1\n            continue",
   "replacement": "        if \"novelty\" not in c:\n            vals.append(0.0)\n            continue",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "recon :: pre-tag loops pooled into the cohort (the retraction defect)",
   "file": "src/discretion_readout.py",
   "anchor": "    cohort = loops[first_idx:]",
   "replacement": "    cohort = loops",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "recon :: pre-cohort count is a constant",
   "file": "src/discretion_readout.py",
   "anchor": "    out[\"pre_cohort_loops\"] = first_idx",
   "replacement": "    out[\"pre_cohort_loops\"] = 0",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "recon :: unparsable started_at floats to the cohort front",
   "file": "src/discretion_readout.py",
   "anchor": "        except Exception:\n            return None\n        return ts if ts.tzinfo else ts.replace(tzinfo=timezone.utc)",
   "replacement": "        except Exception:\n            return datetime.min.replace(tzinfo=timezone.utc)\n        return ts if ts.tzinfo else ts.replace(tzinfo=timezone.utc)",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "recon :: loops ordered by string, not instant",
   "file": "src/discretion_readout.py",
   "anchor": "    loops.sort(key=lambda l: l[\"started_at\"])",
   "replacement": "    loops.sort(key=lambda l: str(l[\"started_at\"]))",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "recon :: schema-invalid files silently dropped",
   "file": "src/discretion_readout.py",
   "anchor": "        if ts is None or not isinstance(rows, list):\n            files_failed += 1\n            continue",
   "replacement": "        if ts is None or not isinstance(rows, list):\n            continue",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "report :: no-tag recon section hides its failed files",
   "file": "src/discretion_readout.py",
   "anchor": "        if rc.get(\"files_failed\"):\n            lines.append(f\"  ({rc['files_failed']} loop log(s) unreadable or \"\n                         \"invalid — the zero above could be incomplete\")",
   "replacement": "        if False:\n            lines.append(f\"  ({rc['files_failed']} loop log(s) unreadable or \"\n                         \"invalid — the zero above could be incomplete\")",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "report :: input coverage UNREADABLE arm dead",
   "file": "src/discretion_readout.py",
   "anchor": "        if cov.get(\"files_failed\"):",
   "replacement": "        if False:",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "report :: not-computable section silently dropped",
   "file": "src/discretion_readout.py",
   "anchor": "    for item in p[\"not_computable\"]:",
   "replacement": "    for item in []:",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "effort :: step-cost loss never surfaced in the payload",
   "file": "src/discretion_readout.py",
   "anchor": "        rows_dropped = _skip.dropped",
   "replacement": "        rows_dropped = 0",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "effort :: loss note renders even on a clean read",
   "file": "src/discretion_readout.py",
   "anchor": "    if eff.get(\"rows_dropped\"):",
   "replacement": "    if True:",
   "tests": "tests/test_discretion_readout.py"},

  {"name": "effort :: the day window cap is dead",
   "file": "src/discretion_readout.py",
   "anchor": "    recent = sorted(by_day)[-days:]",
   "replacement": "    recent = sorted(by_day)",
   "tests": "tests/test_discretion_readout.py"}
]
