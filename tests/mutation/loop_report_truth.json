[
  {"name": "freeze :: _is_frozen never detects the sentinel",
   "file": "src/loop_report.py",
   "anchor": "    return head.startswith(_FREEZE_SENTINEL_PREFIX)",
   "replacement": "    return False",
   "tests": "tests/test_loop_report.py"},

  {"name": "freeze :: terminal writes emit the running sentinel",
   "file": "src/loop_report.py",
   "anchor": "        if status != \"running\" else",
   "replacement": "        if False else",
   "tests": "tests/test_loop_report.py"},

  {"name": "freeze :: the frozen-check at the write site is bypassed",
   "file": "src/loop_report.py",
   "anchor": "        if path.exists() and _is_frozen(path):",
   "replacement": "        if False:",
   "tests": "tests/test_loop_report.py"},

  {"name": "byte-safety :: too-broad scan back on a strict whole-file read",
   "file": "src/loop_report.py",
   "anchor": "    rows, skip = read_jsonl_tail_counted(slice_path)",
   "replacement": "    rows = [json.loads(l) for l in slice_path.read_text(encoding=\"utf-8\").splitlines() if l.strip()] if slice_path.is_file() else []\n    skip = SkipReport()",
   "tests": "tests/test_loop_report.py"},

  {"name": "byte-safety :: a torn slice reads as missing (global-log fallback)",
   "file": "src/loop_report.py",
   "anchor": "    if skip.missing:\n        return None",
   "replacement": "    if skip.missing or skip.dropped:\n        return None",
   "tests": "tests/test_loop_report.py"},

  {"name": "honesty :: slice loss note never renders",
   "file": "src/loop_report.py",
   "anchor": "    if not slice_loss:\n        return \"\"",
   "replacement": "    if True:\n        return \"\"",
   "tests": "tests/test_loop_report.py"},

  {"name": "honesty :: slice loss note renders even with nothing lost",
   "file": "src/loop_report.py",
   "anchor": "    if not slice_loss:\n        return \"\"",
   "replacement": "    if slice_loss is None:\n        return \"\"",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: a torn metadata.json vanishes the run again",
   "file": "src/loop_report.py",
   "anchor": "            meta, meta_unreadable = {}, True",
   "replacement": "            continue",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: degraded row loses its unreadable status",
   "file": "src/loop_report.py",
   "anchor": "                      or (\"unreadable\" if meta_unreadable else \"unknown\"),",
   "replacement": "                      or \"unknown\",",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: degraded row loses the on-disk pointer",
   "file": "src/loop_report.py",
   "anchor": "            \"goal\": meta.get(\"prompt\", \"\") or (\n                \"(metadata.json unreadable — run data still on disk in \"\n                f\"{d.name}/)\" if meta_unreadable else \"\"),",
   "replacement": "            \"goal\": meta.get(\"prompt\", \"\"),",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: a torn run_card drops the whole row",
   "file": "src/loop_report.py",
   "anchor": "                card = {}",
   "replacement": "                continue",
   "tests": "tests/test_loop_report.py"},

  {"name": "launder :: index metadata read parses byte-tainted JSON",
   "file": "src/loop_report.py",
   "anchor": "        meta_unreadable = False\n        try:\n            meta = loads_clean(store_text(meta_path))",
   "replacement": "        meta_unreadable = False\n        try:\n            meta = json.loads(store_text(meta_path))",
   "tests": "tests/test_loop_report.py"},

  {"name": "launder :: Outcome panel parses byte-tainted run_card",
   "file": "src/loop_report.py",
   "anchor": "    try:\n        card = loads_clean(store_text(card_path))",
   "replacement": "    try:\n        card = json.loads(store_text(card_path))",
   "tests": "tests/test_loop_report.py"},

  {"name": "honesty :: torn run_card renders as not-yet-curated silence",
   "file": "src/loop_report.py",
   "anchor": "        return (\n            '<h2>Outcome</h2><div class=\"panel\"><div class=\"meta\">'\n            '<span class=\"overcap\">&#9888; run_card.json exists but could '\n            'not be read</span> &mdash; this run WAS curated; its verdict '\n            'is unavailable until the file is repaired</div></div>'\n        )",
   "replacement": "        return \"\"",
   "tests": "tests/test_loop_report.py"},

  {"name": "honesty :: environment panel drops the manifest loss note",
   "file": "src/loop_report.py",
   "anchor": "        if mskip:",
   "replacement": "        if False:",
   "tests": "tests/test_loop_report.py"},

  {"name": "verdict :: goal_achieved False renders as yes",
   "file": "src/loop_report.py",
   "anchor": "    achieved_str = \"yes\" if achieved is True else (\"no\" if achieved is False else \"not verified\")",
   "replacement": "    achieved_str = \"yes\" if achieved is not None else \"not verified\"",
   "tests": "tests/test_loop_report.py"},

  {"name": "verdict :: omitted-artifact count goes silent (R2-5 revert)",
   "file": "src/loop_report.py",
   "anchor": "    if _omitted:",
   "replacement": "    if False:",
   "tests": "tests/test_loop_report.py"},

  {"name": "attribution :: step claims a same-basename artifact it did not write (R2-6 revert)",
   "file": "src/loop_report.py",
   "anchor": "        if src and fp_str != src:\n            continue  # same basename, different file — not the served one",
   "replacement": "        pass",
   "tests": "tests/test_loop_report.py"},

  {"name": "overcap :: too-broad events leak across loops",
   "file": "src/loop_report.py",
   "anchor": "        if loop_id and row.get(\"loop_id\") and row.get(\"loop_id\") != loop_id:\n            continue",
   "replacement": "        pass",
   "tests": "tests/test_loop_report.py"},

  {"name": "llm-calls :: unreadable call record silently skipped",
   "file": "src/loop_report.py",
   "anchor": "        if cm is None:\n            rows.append(\n                f'<tr><td class=\"meta\">?</td><td class=\"meta\" colspan=\"6\">unreadable record</td>'\n                f'<td><a class=\"raw-link\" href=\"{_esc(rel)}\" target=\"_blank\">raw &#8599;</a></td></tr>'\n            )\n            continue",
   "replacement": "        if cm is None:\n            continue",
   "tests": "tests/test_loop_report.py"},

  {"name": "timing :: mixed real/missing ended_ts no longer forces approximate mode",
   "file": "src/loop_report.py",
   "anchor": "    any_missing = any(not getattr(s, \"ended_ts\", \"\") for s in step_outcomes)",
   "replacement": "    any_missing = all(not getattr(s, \"ended_ts\", \"\") for s in step_outcomes)",
   "tests": "tests/test_loop_report.py"},

  {"name": "timing :: markers slot into fabricated approximate windows",
   "file": "src/loop_report.py",
   "anchor": "    if approx:\n        return slots",
   "replacement": "    if False:\n        return slots",
   "tests": "tests/test_loop_report.py"},

  {"name": "finalizing :: badge ignores run-level terminal status",
   "file": "src/loop_report.py",
   "anchor": "    finalizing = status == \"done\" and run_status in (\"\", \"running\")",
   "replacement": "    finalizing = status == \"done\"",
   "tests": "tests/test_loop_report.py"},

  {"name": "finalizing :: frozen pages refresh forever",
   "file": "src/loop_report.py",
   "anchor": "    refresh_html = ('<meta http-equiv=\"refresh\" content=\"30\">'\n                    if status == \"running\" or finalizing else \"\")",
   "replacement": "    refresh_html = '<meta http-equiv=\"refresh\" content=\"30\">'",
   "tests": "tests/test_loop_report.py"},

  {"name": "backfill :: a crashed loop renders as still running",
   "file": "src/loop_report.py",
   "anchor": "    # running anymore; \"interrupted\" is the honest terminal state.\n    if status == \"running\":\n        status = \"interrupted\"",
   "replacement": "    # running anymore; \"interrupted\" is the honest terminal state.",
   "tests": "tests/test_loop_report.py"},

  {"name": "backfill :: frozen reports rewritten without force",
   "file": "src/loop_report.py",
   "anchor": "                if report_path.exists() and not force:\n                    counts[\"skipped\"] += 1\n                    continue",
   "replacement": "",
   "tests": "tests/test_loop_report.py"},

  {"name": "now :: pre-artifact runs fabricate a result",
   "file": "src/loop_report.py",
   "anchor": "            \"result\": \"(result not captured — this run predates the NOW artifact writer)\",",
   "replacement": "            \"result\": \"\",",
   "tests": "tests/test_loop_report.py"},

  {"name": "status :: effective status prefers raw process status",
   "file": "src/loop_report.py",
   "anchor": "    return s.get(\"success_class\") or s.get(\"status\") or \"unknown\"",
   "replacement": "    return s.get(\"status\") or s.get(\"success_class\") or \"unknown\"",
   "tests": "tests/test_loop_report.py"},

  {"name": "status :: index row status prefers process metadata over the card",
   "file": "src/loop_report.py",
   "anchor": "            \"status\": card.get(\"status\") or meta.get(\"status\")",
   "replacement": "            \"status\": meta.get(\"status\") or card.get(\"status\")",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: latest-loop ranking loses its negation (oldest first)",
   "file": "src/loop_report.py",
   "anchor": "                return (-loop_rank[loop_id], name) if loop_id in loop_rank else (1, name)",
   "replacement": "                return (loop_rank[loop_id], name) if loop_id in loop_rank else (1, name)",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: result links escape the run's build/ tree",
   "file": "src/loop_report.py",
   "anchor": "                    if not candidate.is_file() or not result_relpath.startswith(\"build/\"):",
   "replacement": "                    if not candidate.is_file():",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: torn run_card marker never renders (r1 Architect)",
   "file": "src/loop_report.py",
   "anchor": "        if s.get(\"card_unreadable\"):",
   "replacement": "        if False:",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: row status loses the process-metadata fallback (r1 QA de-vacuize)",
   "file": "src/loop_report.py",
   "anchor": "            \"status\": card.get(\"status\") or meta.get(\"status\")\n                      or (\"unreadable\" if meta_unreadable else \"unknown\"),",
   "replacement": "            \"status\": card.get(\"status\")\n                      or (\"unreadable\" if meta_unreadable else \"unknown\"),",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: totals last-loop-wins instead of accumulating",
   "file": "src/loop_report.py",
   "anchor": "                totals[\"tokens_in\"] += t.get(\"tokens_in\", 0)",
   "replacement": "                totals[\"tokens_in\"] = t.get(\"tokens_in\", 0)",
   "tests": "tests/test_loop_report.py"},

  {"name": "index :: the run count is a constant",
   "file": "src/loop_report.py",
   "anchor": "<div><h1>Runs</h1><div class=\"meta\">{len(summaries)} run(s), newest first</div></div>",
   "replacement": "<div><h1>Runs</h1><div class=\"meta\">0 run(s), newest first</div></div>",
   "tests": "tests/test_loop_report.py"},

  {"name": "search :: bare until-date excludes the whole day",
   "file": "src/loop_report.py",
   "anchor": "        return value + (\"T23:59:59.999999\" if end_of_day else \"T00:00:00\")",
   "replacement": "        return value",
   "tests": "tests/test_loop_report.py"},

  {"name": "debounce :: the window check is dead",
   "file": "src/loop_report.py",
   "anchor": "                if now - last < _INDEX_DEBOUNCE_SECONDS:",
   "replacement": "                if False:",
   "tests": "tests/test_loop_report.py"},

  {"name": "debounce :: force no longer bypasses the window",
   "file": "src/loop_report.py",
   "anchor": "        if not force:",
   "replacement": "        if True:",
   "tests": "tests/test_loop_report.py"},

  {"name": "lane :: attributed markers get the user-lane filter (audience decree overreach)",
   "file": "src/loop_report.py",
   "anchor": "        attributed = [e for e in slice_entries if e.get(\"loop_id\") == loop_id]\n        activity = _user_lane(",
   "replacement": "        attributed = _user_lane([e for e in slice_entries if e.get(\"loop_id\") == loop_id])\n        activity = _user_lane(",
   "tests": "tests/test_loop_report.py"}
]
