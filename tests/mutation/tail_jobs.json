[
 {
  "name": "a run-dir that does not exist is recorded into anyway",
  "file": "src/tail_jobs.py",
  "anchor": "    if not rd.exists():",
  "replacement": "    if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "seq never advances, so the second job overwrites the first",
  "file": "src/tail_jobs.py",
  "anchor": "    return (max(seqs) + 1) if seqs else 1",
  "replacement": "    return 1",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "maintenance may drain before learning",
  "file": "src/tail_jobs.py",
  "anchor": "_KIND_ORDER = {KIND_LEARNING: 0, KIND_MAINTENANCE: 1}",
  "replacement": "_KIND_ORDER = {KIND_LEARNING: 1, KIND_MAINTENANCE: 0}",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a job marked done stays pending, so every drain repeats it",
  "file": "src/tail_jobs.py",
  "anchor": "    pending = [jobs[s] for s in sorted(jobs) if s not in done]",
  "replacement": "    pending = [jobs[s] for s in sorted(jobs)]",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a claim stamped on another host counts as live here",
  "file": "src/tail_jobs.py",
  "anchor": "        if host and host != _hostname():",
  "replacement": "        if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a released claim goes on blocking the run it finished",
  "file": "src/tail_jobs.py",
  "anchor": "    if claim.get(\"released_at\"):",
  "replacement": "    if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "EPERM from kill(pid, 0) reads as a dead process",
  "file": "src/tail_jobs.py",
  "anchor": "    except PermissionError:\n        return True",
  "replacement": "    except PermissionError:\n        return False",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the standing claim is never consulted, so two drainers overlap",
  "file": "src/tail_jobs.py",
  "anchor": "    if respect_claim and _live_claim(st[\"claim\"]):",
  "replacement": "    if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a job that raised is left pending for a sweep to half-repeat",
  "file": "src/tail_jobs.py",
  "anchor": "    if not _append(path, {\"event\": \"done\", \"seq\": seq, \"ok\": ok,\n                          \"error\": err, \"finished_at\": _now()}):",
  "replacement": "    if True:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "an unknown job kind is skipped in silence, not recorded",
  "file": "src/tail_jobs.py",
  "anchor": "        _append(path, {\"event\": \"done\", \"seq\": seq, \"ok\": False,\n                       \"error\": f\"unknown kind {kind!r}\",\n                       \"finished_at\": _now()})",
  "replacement": "        pass",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the surface refresh runs even when no job ran",
  "file": "src/tail_jobs.py",
  "anchor": "    if ran and refresh:",
  "replacement": "    if refresh:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the kinds filter is ignored, so the early drain takes the whole tail",
  "file": "src/tail_jobs.py",
  "anchor": "                if wanted is None or str(j.get(\"kind\")) in wanted]",
  "replacement": "                if True]",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a step row's unknown fields are handed to the constructor",
  "file": "src/tail_jobs.py",
  "anchor": "    kwargs = {k: v for k, v in row.items() if k in names}",
  "replacement": "    kwargs = dict(row)",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "step outcomes travel by the lossy fallback shape",
  "file": "src/tail_jobs.py",
  "anchor": "            rows.append(dataclasses.asdict(s))",
  "replacement": "            raise TypeError(\"forced onto the fallback shape\")",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the recorded adapter identity drops the model",
  "file": "src/tail_jobs.py",
  "anchor": "        \"model_key\": str(getattr(adapter, \"model_key\", \"\") or \"\"),",
  "replacement": "        \"model_key\": \"\",",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a base/failover identity is replayed as a buildable backend",
  "file": "src/tail_jobs.py",
  "anchor": "    if backend and backend not in {\"base\", \"failover\"}:",
  "replacement": "    if backend:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "no default-adapter attempt, so a failed rebuild means no tail at all",
  "file": "src/tail_jobs.py",
  "anchor": "    attempts.append({})",
  "replacement": "    pass",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the child inherits the parent's stdout",
  "file": "src/tail_jobs.py",
  "anchor": "                stdout=out_fh,",
  "replacement": "                stdout=None,",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the child stays in the parent's process group",
  "file": "src/tail_jobs.py",
  "anchor": "                start_new_session=True,",
  "replacement": "                start_new_session=False,",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the child is left to find src/ on its own",
  "file": "src/tail_jobs.py",
  "anchor": "    env[\"PYTHONPATH\"] = (f\"{src_dir}{os.pathsep}{existing}\"\n                         if existing else src_dir)",
  "replacement": "    pass",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "spawn is attempted whatever the config says",
  "file": "src/tail_jobs.py",
  "anchor": "    if spawn_enabled():",
  "replacement": "    if True:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a spawn that returned no pid is reported as spawned",
  "file": "src/tail_jobs.py",
  "anchor": "        if pid is not None:",
  "replacement": "        if True:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a run with nothing pending still dispatches a tail",
  "file": "src/tail_jobs.py",
  "anchor": "    if not pending_jobs(handle_id):",
  "replacement": "    if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the config default is ON, so a fresh install inherits the spawn",
  "file": "src/tail_jobs.py",
  "anchor": "        return _strict_bool(_cfg_get(\"tail.spawn\", False), False)",
  "replacement": "        return _strict_bool(_cfg_get(\"tail.spawn\", True), True)",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a young tail is swept as stranded, racing the child that owns it",
  "file": "src/tail_jobs.py",
  "anchor": "        if age < min_age_s:",
  "replacement": "        if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the sweep's dry run drains anyway",
  "file": "src/tail_jobs.py",
  "anchor": "        if dry_run or not item[\"drainable\"]:\n            continue",
  "replacement": "        if not item[\"drainable\"]:\n            continue",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "one torn byte takes the whole job store with it",
  "file": "src/tail_jobs.py",
  "anchor": "        return read_jsonl_announced(path, \"tail_jobs\")",
  "replacement": "        return [json.loads(line) for line in path.read_text(encoding=\"utf-8\").splitlines() if line.strip()]",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a stranded run with a live claim is drained out from under it",
  "file": "src/tail_jobs.py",
  "anchor": "        if _live_claim(st[\"claim\"]):\n            continue",
  "replacement": "        if False:\n            continue",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "sequence is allocated outside the store lock (the r1 race)",
  "file": "src/tail_jobs.py",
  "anchor": "    recorded, _ = _transact(path, lambda rows: ({\n        \"event\": \"job\",\n        \"seq\": _next_seq(rows),\n        \"kind\": KIND_LEARNING,",
  "replacement": "    recorded = _append(path, ({\n        \"event\": \"job\",\n        \"seq\": _next_seq(_read_rows(path) or []),\n        \"kind\": KIND_LEARNING,",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the claim is decided outside the store lock (the r1 race)",
  "file": "src/tail_jobs.py",
  "anchor": "    claimed, value = _transact(path, _claim)",
  "replacement": "    _rows_unlocked = _read_rows(path) or []\n    _row_unlocked, value = _claim(_rows_unlocked)\n    claimed = bool(_row_unlocked) and _append(path, _row_unlocked)",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the transaction appends without re-reading under the lock",
  "file": "src/tail_jobs.py",
  "anchor": "            rows = _read_rows(path)\n            if rows is None:\n                return False, None",
  "replacement": "            rows = []\n            if rows is None:\n                return False, None",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the inline lane rebuilds the adapter instead of using the live one",
  "file": "src/tail_jobs.py",
  "anchor": "    job_adapter = (adapter if adapter is not None\n                   else _LIVE_ADAPTERS.get(str(handle_id))\n                   or _build_adapter(spec))",
  "replacement": "    job_adapter = adapter if adapter is not None else _build_adapter(spec)",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the live adapter is never released, so the cache leaks",
  "file": "src/tail_jobs.py",
  "anchor": "    _forget_adapter(handle_id)",
  "replacement": "    pass",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a claim that could not be published still drains",
  "file": "src/tail_jobs.py",
  "anchor": "    if not claimed:",
  "replacement": "    if False:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "an unrecordable completion passes in silence",
  "file": "src/tail_jobs.py",
  "anchor": "        log.error(\"tail_jobs: %s job (seq %s) for %s RAN but its completion \"",
  "replacement": "        log.debug(\"tail_jobs: %s job (seq %s) for %s RAN but its completion \"",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the job runs with no run pinned, so record_llm_call no-ops",
  "file": "src/tail_jobs.py",
  "anchor": "        _pin = _scoped_run(_rd_for_pin(str(handle_id)))",
  "replacement": "        from contextlib import nullcontext as _nc\n        _pin = _nc()",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the run pin is left set, contaminating the sweep's own process",
  "file": "src/tail_jobs.py",
  "anchor": "        from runs import scoped_run_dir as _scoped_run, run_dir as _rd_for_pin",
  "replacement": "        from contextlib import nullcontext\n        from runs import run_dir as _rd_for_pin, set_current_run_dir as _scr\n        def _scoped_run(p):\n            _scr(p)\n            return nullcontext()",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "maintenance is re-run after a drain already started",
  "file": "src/tail_jobs.py",
  "anchor": "    if kind == KIND_LEARNING:\n        return True\n    return not started",
  "replacement": "    return True",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "an unreleased claim is not read as a started drain",
  "file": "src/tail_jobs.py",
  "anchor": "            return not r.get(\"released_at\")",
  "replacement": "            return False",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the sweep runs everything it found, not only what is safe",
  "file": "src/tail_jobs.py",
  "anchor": "        if dry_run or not item[\"drainable\"]:",
  "replacement": "        if dry_run:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the sweep drains the whole job set, ignoring the safe subset",
  "file": "src/tail_jobs.py",
  "anchor": "            drained += run_jobs(item[\"handle_id\"],\n                                kinds=tuple(item[\"drainable\"]))",
  "replacement": "            drained += run_jobs(item[\"handle_id\"])",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the candidate list is truncated before stranded runs are filtered",
  "file": "src/tail_jobs.py",
  "anchor": "    for p in candidates:\n        if len(out) >= max(1, limit):",
  "replacement": "    for p in candidates[::-1][:max(1, limit) * 4]:\n        if len(out) >= max(1, limit):",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "an unreadable job store is classified as not stranded",
  "file": "src/tail_jobs.py",
  "anchor": "        rows = _read_rows(p)\n        if rows is None:      # unreadable != \"nothing here\"",
  "replacement": "        rows = _read_rows(p) or []\n        if False:      # unreadable != \"nothing here\"",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a quoted \"false\" enables the spawn (plain truthiness)",
  "file": "src/tail_jobs.py",
  "anchor": "        return _strict_bool(_cfg_get(\"tail.spawn\", False), False)",
  "replacement": "        return bool(_cfg_get(\"tail.spawn\", False))",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "an uninterpretable flag value is read as ON",
  "file": "src/tail_jobs.py",
  "anchor": "    log.warning(\"tail_jobs: uninterpretable flag value %r \u2014 using %s\",\n                value, default)\n    return default",
  "replacement": "    return True",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "an unreadable store is recorded into as though it were empty",
  "file": "src/tail_jobs.py",
  "anchor": "    if path is None:\n        return None\n    if not path.exists():\n        return []",
  "replacement": "    if path is None:\n        return None\n    if not path.exists():\n        return []\n    if True:\n        pass",
  "tests": "tests/test_tail_jobs.py",
  "equivalent": "a no-op inserted after the two real branches; kept only to prove the anchor still binds the missing-vs-unreadable split"
 },
 {
  "name": "a failed job is not collected, so nothing surfaces it",
  "file": "src/tail_jobs.py",
  "anchor": "            if r.get(\"ok\") is False:\n                failed.append(r)",
  "replacement": "            if False:\n                failed.append(r)",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "the sweep reports no failed jobs",
  "file": "src/tail_jobs.py",
  "anchor": "        \"failed_jobs\": [i[\"handle_id\"] for i in found if i[\"failed\"]],",
  "replacement": "        \"failed_jobs\": [],",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a run with only failed jobs is skipped by the sweep entirely",
  "file": "src/tail_jobs.py",
  "anchor": "        if not st[\"pending\"] and not st[\"failed\"]:",
  "replacement": "        if not st[\"pending\"]:",
  "tests": "tests/test_tail_jobs.py"
 },
 {
  "name": "a failed surface refresh is swallowed at debug level again",
  "file": "src/tail_jobs.py",
  "anchor": "        if path is not None:\n            _append(path, {\"event\": \"refresh\", \"ok\": False,",
  "replacement": "        if False:\n            _append(path, {\"event\": \"refresh\", \"ok\": False,",
  "tests": "tests/test_tail_jobs.py"
 }
]
